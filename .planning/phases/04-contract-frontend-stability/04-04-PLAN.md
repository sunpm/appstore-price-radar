---
phase: 04-contract-frontend-stability
plan: '04'
type: execute
wave: 3
depends_on:
  - '01'
  - '03'
files_modified:
  - packages/contracts/src/prices.ts
  - apps/worker/src/lib/appstore.ts
  - apps/worker/src/lib/appstore.types.ts
  - apps/worker/src/lib/checker.ts
  - apps/worker/src/db/schema.ts
  - apps/worker/drizzle/0004_app_snapshot_metadata.sql
  - apps/worker/src/services/prices.ts
  - apps/worker/test/appstore.lookup.test.ts
  - apps/worker/test/prices.history.test.ts
  - apps/web/src/views/app/types.ts
  - apps/web/src/views/app/AppDetailView.vue
  - apps/web/src/views/app/components/AppDetailHeroCard.vue
  - apps/web/src/views/app/components/AppDetailDecisionStats.vue
  - apps/web/src/views/app/components/AppDetailMetadataPanel.vue
autonomous: true
requirements:
  - API-05
user_setup: []
must_haves:
  truths:
    - 详情页必须首屏展示影响决策的核心 App 信息，不能只停留在 `appName + icon + price history`。
    - 长尾 metadata 必须折叠展示，而不是继续把所有字段平铺在主内容流里。
  artifacts:
    - apps/worker/src/lib/appstore.ts
    - apps/worker/src/db/schema.ts
    - apps/worker/drizzle/0004_app_snapshot_metadata.sql
    - apps/worker/src/services/prices.ts
    - apps/web/src/views/app/AppDetailView.vue
    - apps/web/src/views/app/components/AppDetailMetadataPanel.vue
  key_links:
    - `apps/worker/src/lib/appstore.ts` 当前只保留 price/name/icon/url，执行时要把决策相关 metadata 一并纳入 lookup 结果和 snapshot。
    - `AppDetailView.vue` 应按 Vue 最佳实践变成组合容器，细分为 hero / stats / metadata panel 等子组件，而不是继续堆叠在单文件里。
---

<objective>
补齐详情页做决策需要的 App metadata，并把页面信息层级重构成“首屏核心信息 + 折叠长尾信息”，让用户更快判断是购买、继续观望还是继续订阅。

Purpose: `API-05` 的关键不是“字段越多越好”，而是让数据源和 UI 层级都围绕决策信息组织。
Output: 扩展后的 App Store metadata schema / snapshot persistence、更新后的 detail DTO，以及分层化的 AppDetail 页面组件。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-contract-frontend-stability/04-RESEARCH.md
@.planning/phases/04-contract-frontend-stability/04-VALIDATION.md
@packages/contracts/src/prices.ts
@apps/worker/src/lib/appstore.ts
@apps/worker/src/lib/appstore.types.ts
@apps/worker/src/lib/checker.ts
@apps/worker/src/db/schema.ts
@apps/worker/src/services/prices.ts
@apps/web/src/views/app/AppDetailView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 App Store metadata 采集、snapshot schema 和 detail DTO</name>
  <files>packages/contracts/src/prices.ts, apps/worker/src/lib/appstore.ts, apps/worker/src/lib/appstore.types.ts, apps/worker/src/lib/checker.ts, apps/worker/src/db/schema.ts, apps/worker/drizzle/0004_app_snapshot_metadata.sql, apps/worker/src/services/prices.ts, apps/worker/test/appstore.lookup.test.ts, apps/worker/test/prices.history.test.ts</files>
  <read_first>packages/contracts/src/prices.ts, apps/worker/src/lib/appstore.ts, apps/worker/src/lib/appstore.types.ts, apps/worker/src/lib/checker.ts, apps/worker/src/db/schema.ts, apps/worker/drizzle/0003_auth_security_limits.sql, apps/worker/src/services/prices.ts, apps/worker/test/appstore.lookup.test.ts</read_first>
  <action>Extend `apps/worker/src/lib/appstore.ts` lookup schema to read and validate exact metadata fields `sellerName`, `primaryGenreName`, `description`, `averageUserRating`, `userRatingCount`, `bundleId`, `version`, `minimumOsVersion`, and `releaseNotes` from the App Store lookup response. Update `apps/worker/src/lib/appstore.types.ts` so both found/invalid-price variants carry these metadata fields. Extend `apps/worker/src/db/schema.ts` `appSnapshots` table with matching nullable columns for these metadata values, and create `apps/worker/drizzle/0004_app_snapshot_metadata.sql` with the corresponding `ALTER TABLE app_snapshots ADD COLUMN IF NOT EXISTS ...` statements. Update `apps/worker/src/lib/checker.ts` snapshot upsert logic so these metadata fields are persisted on refresh. Extend `packages/contracts/src/prices.ts` with exact DTOs `AppDecisionMetadataDto` and `AppDetailResponseDto`, and update `apps/worker/src/services/prices.ts` to return metadata under a dedicated `detail` or `metadata` section instead of forcing the Web to infer from raw snapshot fields. Extend `apps/worker/test/appstore.lookup.test.ts` and `apps/worker/test/prices.history.test.ts` so the metadata mapping and response shape are asserted explicitly.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/appstore.lookup.test.ts test/prices.history.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/lib/appstore.ts` contains `sellerName`
    - `apps/worker/src/lib/appstore.ts` contains `averageUserRating`
    - `apps/worker/src/db/schema.ts` contains `sellerName:`
    - `apps/worker/src/db/schema.ts` contains `primaryGenreName:`
    - `apps/worker/drizzle/0004_app_snapshot_metadata.sql` contains `ALTER TABLE app_snapshots`
    - `packages/contracts/src/prices.ts` contains `export interface AppDecisionMetadataDto`
    - `packages/contracts/src/prices.ts` contains `export interface AppDetailResponseDto`
    - `apps/worker/test/appstore.lookup.test.ts` contains `maps metadata fields`
  </acceptance_criteria>
  <done>详情页需要的 metadata 已从采集、落库到 DTO 打通，前端不再只拿到最小化 snapshot 信息。</done>
</task>

<task type="auto">
  <name>Task 2: 将详情页重构为决策优先的分层展示，并折叠长尾元数据</name>
  <files>apps/web/src/views/app/types.ts, apps/web/src/views/app/AppDetailView.vue, apps/web/src/views/app/components/AppDetailHeroCard.vue, apps/web/src/views/app/components/AppDetailDecisionStats.vue, apps/web/src/views/app/components/AppDetailMetadataPanel.vue</files>
  <read_first>apps/web/src/views/app/types.ts, apps/web/src/views/app/AppDetailView.vue, apps/web/src/views/profile/components/ProfileHistorySection.vue, packages/contracts/src/prices.ts, apps/web/src/composables/usePriceHistory.ts</read_first>
  <action>Update `apps/web/src/views/app/types.ts` so it consumes the new `AppDetailResponseDto` from the shared contracts package. Refactor `AppDetailView.vue` into a thin composition surface that loads detail data and delegates UI to three child components: `AppDetailHeroCard.vue` for app identity/current price/updated time/App Store CTA, `AppDetailDecisionStats.vue` for rating、评价数、分类、距高点跌幅、最低价等首屏决策指标, and `AppDetailMetadataPanel.vue` for folded long-tail metadata such as bundle id、版本、最低系统版本、发行说明和描述摘要. Ensure the folded panel has an explicit default-collapsed state and a visible action label `展开更多应用信息` / `收起扩展信息`, while preserving the existing price history chart and change table below the decision area.</action>
  <verify>pnpm typecheck && pnpm lint</verify>
  <acceptance_criteria>
    - `apps/web/src/views/app/AppDetailView.vue` contains `AppDetailHeroCard`
    - `apps/web/src/views/app/AppDetailView.vue` contains `AppDetailDecisionStats`
    - `apps/web/src/views/app/AppDetailView.vue` contains `AppDetailMetadataPanel`
    - `apps/web/src/views/app/components/AppDetailMetadataPanel.vue` contains `展开更多应用信息`
    - `apps/web/src/views/app/components/AppDetailMetadataPanel.vue` contains `收起扩展信息`
    - `apps/web/src/views/app/components/AppDetailDecisionStats.vue` contains `距高点跌幅`
    - `apps/web/src/views/app/components/AppDetailHeroCard.vue` contains `前往 App Store`
  </acceptance_criteria>
  <done>详情页已经形成“首屏决策信息 + 折叠长尾信息”的稳定展示层级，用户不用先扫一长串字段才能做判断。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/appstore.lookup.test.ts test/prices.history.test.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "sellerName|averageUserRating|AppDetailResponseDto|AppDetailHeroCard|AppDetailDecisionStats|AppDetailMetadataPanel|展开更多应用信息" packages/contracts apps/worker/src apps/web/src`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/04-contract-frontend-stability/04-04-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- App detail metadata is collected, stored, and exposed through the shared DTO
- Detail page first screen surfaces decision-critical information
- Long-tail metadata is folded instead of dumped inline
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/04-contract-frontend-stability/04-04-SUMMARY.md`
</output>
