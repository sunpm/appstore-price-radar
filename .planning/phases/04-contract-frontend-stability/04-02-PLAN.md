---
phase: 04-contract-frontend-stability
plan: '02'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - apps/web/src/lib/http.ts
  - apps/web/src/lib/auth-session.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/composables/useAuthSession.ts
  - apps/web/src/composables/useAuthedApi.ts
  - apps/web/src/router.ts
  - apps/web/src/layouts/MainLayout.vue
  - apps/web/src/views/auth/AuthView.vue
  - apps/web/src/views/profile/ProfileView.vue
  - apps/web/src/views/security/SecurityView.vue
  - apps/web/src/views/auth/composables/useAuthFeedback.ts
autonomous: true
requirements:
  - AUTH-04
  - API-04
user_setup: []
must_haves:
  truths:
    - 需要登录的请求必须共用一套 token 注入、401 清理、错误解析和重新登录引导逻辑。
    - `AuthView`、`ProfileView`、`SecurityView` 不应继续各自维护一份同构 `apiRequest()` 和 session restore 流程。
  artifacts:
    - apps/web/src/lib/api-client.ts
    - apps/web/src/composables/useAuthSession.ts
    - apps/web/src/composables/useAuthedApi.ts
    - apps/web/src/views/auth/AuthView.vue
    - apps/web/src/views/profile/ProfileView.vue
    - apps/web/src/views/security/SecurityView.vue
  key_links:
    - `apps/web/src/router.ts` 目前只检查“有没有 token 字符串”，执行时要与共享 session/unauthorized 行为保持一致。
    - `apps/web/src/layouts/MainLayout.vue` 已监听 token storage 事件，新的 session composable 必须复用而不是绕开这个机制。
---

<objective>
把前端登录态恢复、鉴权请求和 `401` 错误反馈从页面里抽出来，做成共享逻辑，让认证页、工作台和安全页面对会话失效时表现一致。

Purpose: `AUTH-04` / `API-04` 的关键是“同一类错误由同一条链路处理”，而不是每个页面各写一个 `clearSession()`。
Output: 共享 auth session composable、共享 authed api helper，以及已收敛的 `AuthView` / `ProfileView` / `SecurityView`。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-contract-frontend-stability/04-RESEARCH.md
@.planning/phases/04-contract-frontend-stability/04-VALIDATION.md
@apps/web/src/lib/http.ts
@apps/web/src/lib/auth-session.ts
@apps/web/src/router.ts
@apps/web/src/layouts/MainLayout.vue
@apps/web/src/views/auth/AuthView.vue
@apps/web/src/views/profile/ProfileView.vue
@apps/web/src/views/security/SecurityView.vue
@apps/web/src/views/auth/composables/useAuthFeedback.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立共享 session / authed request 基础设施</name>
  <files>apps/web/src/lib/http.ts, apps/web/src/lib/auth-session.ts, apps/web/src/lib/api-client.ts, apps/web/src/composables/useAuthSession.ts, apps/web/src/composables/useAuthedApi.ts</files>
  <read_first>apps/web/src/lib/http.ts, apps/web/src/lib/auth-session.ts, apps/web/src/views/auth/AuthView.vue, apps/web/src/views/profile/ProfileView.vue, apps/web/src/views/security/SecurityView.vue, apps/web/src/router.ts, apps/web/src/layouts/MainLayout.vue</read_first>
  <action>Create `apps/web/src/lib/api-client.ts` with a shared request helper that wraps `fetch(buildApiUrl(...))`, sets `content-type` for JSON bodies, parses API errors via `parseApiErrorText`, and accepts exact options `{ auth?: boolean, onUnauthorized?: () => Promise<void> | void, signal?: AbortSignal }`. Create `apps/web/src/composables/useAuthSession.ts` that exposes exact members `token`, `currentUser`, `sessionExpiresAt`, `restoreSession()`, `applySession()`, `clearSession()`, and `isAuthenticated`, all backed by `auth-session.ts`. Create `apps/web/src/composables/useAuthedApi.ts` that composes the shared request helper with `useAuthSession()` and centralizes the exact unauthorized message `登录状态已失效，请重新登录。`. If needed, extend `apps/web/src/lib/http.ts` to export a pure helper for normalizing API error payloads without coupling unauthorized behavior to views.</action>
  <verify>pnpm typecheck && pnpm lint</verify>
  <acceptance_criteria>
    - `apps/web/src/lib/api-client.ts` contains `export async function apiRequest`
    - `apps/web/src/composables/useAuthSession.ts` contains `restoreSession`
    - `apps/web/src/composables/useAuthSession.ts` contains `isAuthenticated`
    - `apps/web/src/composables/useAuthedApi.ts` contains `登录状态已失效，请重新登录。`
    - `apps/web/src/composables/useAuthedApi.ts` contains `onUnauthorized`
  </acceptance_criteria>
  <done>共享 session 与鉴权请求基础设施已就绪，页面可以改为消费同一条 unauthorized 处理链。</done>
</task>

<task type="auto">
  <name>Task 2: 重构认证页、工作台和安全页，统一 401 清理、toast 和跳转行为</name>
  <files>apps/web/src/router.ts, apps/web/src/layouts/MainLayout.vue, apps/web/src/views/auth/AuthView.vue, apps/web/src/views/profile/ProfileView.vue, apps/web/src/views/security/SecurityView.vue, apps/web/src/views/auth/composables/useAuthFeedback.ts</files>
  <read_first>apps/web/src/router.ts, apps/web/src/layouts/MainLayout.vue, apps/web/src/views/auth/AuthView.vue, apps/web/src/views/profile/ProfileView.vue, apps/web/src/views/security/SecurityView.vue, apps/web/src/composables/useAuthSession.ts, apps/web/src/composables/useAuthedApi.ts, apps/web/src/views/auth/composables/useAuthFeedback.ts</read_first>
  <action>Refactor `AuthView.vue`, `ProfileView.vue`, and `SecurityView.vue` to remove their local `apiRequest()` duplicates and replace direct token/session state with the shared composables from Task 1. Keep route views thinner by leaving UI-specific state in the view and moving session/request side effects out. Update `router.ts` so `beforeEach` still guards `meta.requiresAuth`, but uses the shared stored-token/session semantics rather than page-local assumptions. Update `MainLayout.vue` so auth modal visibility and nav state still respond to token changes through the same shared session source. Harmonize unauthorized feedback across auth/profile/security flows using the exact user-facing copy `登录状态已失效，请重新登录。`, while preserving special-case password error copy such as `当前密码不正确。若你刚完成重置密码，请输入最新密码。`.</action>
  <verify>pnpm typecheck && pnpm lint</verify>
  <acceptance_criteria>
    - `apps/web/src/views/auth/AuthView.vue` does not contain `async function apiRequest`
    - `apps/web/src/views/profile/ProfileView.vue` does not contain `async function apiRequest`
    - `apps/web/src/views/security/SecurityView.vue` does not contain `async function apiRequest`
    - `apps/web/src/views/profile/ProfileView.vue` contains `登录状态已失效，请重新登录。`
    - `apps/web/src/views/security/SecurityView.vue` contains `当前密码不正确。若你刚完成重置密码，请输入最新密码。`
    - `apps/web/src/router.ts` contains `requiresAuth`
  </acceptance_criteria>
  <done>认证页、工作台和安全页已共享同一套 unauthorized 行为，401 不会再在不同页面表现出分叉体验。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "async function apiRequest|useAuthSession|useAuthedApi|登录状态已失效，请重新登录。" apps/web/src`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/04-contract-frontend-stability/04-02-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- Frontend auth/session request handling is shared, not view-local
- 401 behavior is consistent across auth, profile, and security pages
- Route guards and nav state still work after extraction
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/04-contract-frontend-stability/04-02-SUMMARY.md`
</output>
