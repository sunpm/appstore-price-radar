---
phase: 04-contract-frontend-stability
plan: '03'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - packages/contracts/src/prices.ts
  - apps/worker/src/constants/routes.ts
  - apps/worker/src/routes/prices.ts
  - apps/worker/src/services/prices.ts
  - apps/worker/src/services/prices.types.ts
  - apps/worker/test/prices.history.test.ts
  - apps/web/src/views/profile/ProfileView.vue
  - apps/web/src/views/profile/components/ProfileHistorySection.vue
  - apps/web/src/views/app/AppDetailView.vue
  - apps/web/src/composables/usePriceHistory.ts
autonomous: true
requirements:
  - API-02
user_setup: []
must_haves:
  truths:
    - 历史查询必须从“一次性全量拉取”改成“按窗口和分页渐进加载”，否则 payload 和页面重算成本会持续增长。
    - 频繁切换订阅或详情页时，旧请求不能覆盖新请求结果，且已加载历史应可复用缓存。
  artifacts:
    - apps/worker/src/routes/prices.ts
    - apps/worker/src/services/prices.ts
    - apps/worker/test/prices.history.test.ts
    - apps/web/src/composables/usePriceHistory.ts
    - apps/web/src/views/profile/ProfileView.vue
    - apps/web/src/views/app/AppDetailView.vue
  key_links:
    - `apps/web/src/views/profile/ProfileView.vue` 和 `apps/web/src/views/app/AppDetailView.vue` 当前都包含 `limit=3650`，执行后这条硬编码必须消失。
    - `packages/contracts/src/prices.ts` 的分页 DTO 要成为 Worker route 和 Web composable 的共同契约。
---

<objective>
降低价格历史接口和图表渲染的负担，把工作台和详情页从“全量拉历史”改成“窗口化 + 分页 + 可取消 + 可缓存”的稳定读取路径。

Purpose: `API-02` 要解决的不只是请求体大，还包括频繁切换时的旧请求覆盖、新请求重算和页面卡顿。
Output: 新的 history page DTO、分页化 Worker query、共享前端 history composable 与更新后的工作台/详情页加载策略。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-contract-frontend-stability/04-RESEARCH.md
@.planning/phases/04-contract-frontend-stability/04-VALIDATION.md
@packages/contracts/src/prices.ts
@apps/worker/src/constants/routes.ts
@apps/worker/src/routes/prices.ts
@apps/worker/src/services/prices.ts
@apps/worker/src/services/prices.types.ts
@apps/web/src/views/profile/ProfileView.vue
@apps/web/src/views/profile/components/ProfileHistorySection.vue
@apps/web/src/views/app/AppDetailView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为价格历史接口增加 window / cursor / page meta 契约，并补 Worker 回归测试</name>
  <files>packages/contracts/src/prices.ts, apps/worker/src/constants/routes.ts, apps/worker/src/routes/prices.ts, apps/worker/src/services/prices.ts, apps/worker/src/services/prices.types.ts, apps/worker/test/prices.history.test.ts</files>
  <read_first>packages/contracts/src/prices.ts, apps/worker/src/constants/routes.ts, apps/worker/src/routes/prices.ts, apps/worker/src/services/prices.ts, apps/worker/src/services/prices.types.ts, apps/worker/src/db/schema.ts, apps/worker/test/subscriptions.create.test.ts</read_first>
  <action>Extend `packages/contracts/src/prices.ts` so `PriceHistoryResponseDto` contains exact sections `snapshot`, `history`, `page`, and `summary`. Define `PriceHistoryPageDto` with exact fields `window`, `pageSize`, `nextCursor`, and `hasMore`. Define `PriceHistorySummaryDto` with exact fields `totalChanges`, `latestChangeAt`, and `earliestChangeAt`. Update `apps/worker/src/constants/routes.ts` to add exact constants `PRICE_HISTORY_PAGE_SIZE_DEFAULT = 60`, `PRICE_HISTORY_PAGE_SIZE_MAX = 120`, and allowed window values `30d`, `90d`, `1y`, `all`. Update `apps/worker/src/routes/prices.ts` query validation to accept `window`, `pageSize`, and `cursor` instead of raw `limit`. Update `apps/worker/src/services/prices.ts` to filter history by the requested window, fetch one extra row for pagination, derive `nextCursor` in the format `{changedAtISO}__{id}`, and populate `summary` without loading unbounded history. Add `apps/worker/test/prices.history.test.ts` covering default window, `hasMore/nextCursor`, and invalid cursor fallback or rejection semantics.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/prices.history.test.ts</verify>
  <acceptance_criteria>
    - `packages/contracts/src/prices.ts` contains `interface PriceHistoryPageDto`
    - `packages/contracts/src/prices.ts` contains `window: '30d' | '90d' | '1y' | 'all'`
    - `apps/worker/src/constants/routes.ts` contains `PRICE_HISTORY_PAGE_SIZE_DEFAULT = 60`
    - `apps/worker/src/constants/routes.ts` contains `PRICE_HISTORY_PAGE_SIZE_MAX = 120`
    - `apps/worker/src/routes/prices.ts` contains `pageSize`
    - `apps/worker/src/routes/prices.ts` contains `cursor`
    - `apps/worker/src/services/prices.ts` contains `nextCursor`
    - `apps/worker/test/prices.history.test.ts` contains `returns hasMore and nextCursor`
  </acceptance_criteria>
  <done>Worker history 接口已具备窗口化和分页能力，不再要求前端一次拉回全部历史数据。</done>
</task>

<task type="auto">
  <name>Task 2: 前端接入共享 price history composable，支持缓存、取消和渐进加载</name>
  <files>apps/web/src/composables/usePriceHistory.ts, apps/web/src/views/profile/ProfileView.vue, apps/web/src/views/profile/components/ProfileHistorySection.vue, apps/web/src/views/app/AppDetailView.vue</files>
  <read_first>apps/web/src/views/profile/ProfileView.vue, apps/web/src/views/profile/components/ProfileHistorySection.vue, apps/web/src/views/app/AppDetailView.vue, apps/web/src/composables/useAuthedApi.ts, packages/contracts/src/prices.ts, apps/worker/src/routes/prices.ts</read_first>
  <action>Create `apps/web/src/composables/usePriceHistory.ts` that manages exact state members `history`, `snapshot`, `page`, `summary`, `loading`, `loadingMore`, `selectedWindow`, `loadInitial()`, `loadMore()`, and `abortActiveRequest()`. The composable must cache results by exact key `${appId}:${country}:${window}`, use `AbortController` to cancel stale requests, and read the new `window/pageSize/cursor` API contract. Update `ProfileView.vue` and `AppDetailView.vue` to remove hardcoded `limit=3650` requests and consume the composable instead. Update `ProfileHistorySection.vue` to expose window switching and “加载更多” affordance using the new page metadata, while keeping existing chart/table responsibilities intact.</action>
  <verify>pnpm typecheck && pnpm lint</verify>
  <acceptance_criteria>
    - `apps/web/src/composables/usePriceHistory.ts` contains `AbortController`
    - `apps/web/src/composables/usePriceHistory.ts` contains `loadMore`
    - `apps/web/src/composables/usePriceHistory.ts` contains `selectedWindow`
    - `apps/web/src/views/profile/ProfileView.vue` does not contain `limit=3650`
    - `apps/web/src/views/app/AppDetailView.vue` does not contain `limit=3650`
    - `apps/web/src/views/profile/components/ProfileHistorySection.vue` contains `加载更多`
  </acceptance_criteria>
  <done>工作台和详情页已通过共享 composable 渐进加载历史数据，频繁切换时不会再依赖超长 payload 和无控制的旧请求。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/prices.history.test.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "limit=3650|PRICE_HISTORY_PAGE_SIZE_DEFAULT|nextCursor|selectedWindow|加载更多" packages/contracts apps/worker/src apps/web/src`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/04-contract-frontend-stability/04-03-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- Price history API is windowed and paginated
- Web history loading is cache-aware and abort-safe
- `limit=3650` is removed from profile/detail code paths
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/04-contract-frontend-stability/04-03-SUMMARY.md`
</output>
