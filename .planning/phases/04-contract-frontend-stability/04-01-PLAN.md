---
phase: 04-contract-frontend-stability
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/auth.ts
  - packages/contracts/src/subscriptions.ts
  - packages/contracts/src/prices.ts
  - packages/contracts/src/index.ts
  - apps/worker/src/routes/auth.ts
  - apps/worker/src/routes/subscriptions.ts
  - apps/worker/src/routes/prices.ts
  - apps/worker/src/services/auth.types.ts
  - apps/worker/src/services/subscriptions.types.ts
  - apps/worker/src/services/prices.types.ts
  - apps/worker/src/services/auth.ts
  - apps/worker/src/services/subscriptions.ts
  - apps/worker/src/services/prices.ts
  - apps/web/src/views/auth/types.ts
  - apps/web/src/views/profile/types.ts
  - apps/web/src/views/app/types.ts
  - apps/worker/test/auth.test.ts
  - apps/worker/test/subscriptions.create.test.ts
autonomous: true
requirements:
  - API-01
user_setup: []
must_haves:
  truths:
    - 认证、订阅和价格历史相关 DTO 必须只有一个 TypeScript source of truth，不能再由 Web/Worker 各维护一份。
    - Worker 返回体必须通过显式 mapper 输出共享 DTO，而不是直接回 raw DB rows 或匿名对象。
  artifacts:
    - packages/contracts/src/index.ts
    - packages/contracts/src/auth.ts
    - packages/contracts/src/subscriptions.ts
    - packages/contracts/src/prices.ts
    - apps/worker/src/routes/auth.ts
    - apps/worker/src/routes/subscriptions.ts
    - apps/worker/src/routes/prices.ts
  key_links:
    - `apps/web/src/views/auth/types.ts`、`apps/web/src/views/profile/types.ts`、`apps/web/src/views/app/types.ts` 应改为从共享 contracts re-export，而不是继续手写接口。
    - `apps/worker/src/services/*.types.ts` 仍可保留 service-internal 类型，但 HTTP response body 要对齐共享 DTO 命名。
---

<objective>
为认证、订阅和价格历史建立真正可复用的单一 DTO 来源，先把编译期契约固定下来，再让后续前端行为和接口优化都基于这条 shared surface 继续演进。

Purpose: `API-01` 的关键不是“看起来字段差不多”，而是让 Web 和 Worker 在同一份 contracts 上协作，字段变更时编译期立即暴露。
Output: 新的 `packages/contracts` workspace package、Worker response mapper、Web 端类型 re-export 与相关回归断言。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-contract-frontend-stability/04-RESEARCH.md
@.planning/phases/04-contract-frontend-stability/04-VALIDATION.md
@package.json
@pnpm-workspace.yaml
@apps/web/src/views/auth/types.ts
@apps/web/src/views/profile/types.ts
@apps/web/src/views/app/types.ts
@apps/web/src/types/prices.ts
@apps/worker/src/routes/auth.ts
@apps/worker/src/routes/subscriptions.ts
@apps/worker/src/routes/prices.ts
@apps/worker/src/services/auth.types.ts
@apps/worker/src/services/subscriptions.types.ts
@apps/worker/src/services/prices.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立共享 contracts workspace package，承载 auth / subscription / history DTO</name>
  <files>package.json, pnpm-workspace.yaml, packages/contracts/package.json, packages/contracts/tsconfig.json, packages/contracts/src/auth.ts, packages/contracts/src/subscriptions.ts, packages/contracts/src/prices.ts, packages/contracts/src/index.ts</files>
  <read_first>package.json, pnpm-workspace.yaml, tsconfig.base.json, apps/web/src/views/auth/types.ts, apps/web/src/views/profile/types.ts, apps/web/src/views/app/types.ts, apps/web/src/types/prices.ts, apps/worker/src/services/auth.types.ts, apps/worker/src/services/subscriptions.types.ts, apps/worker/src/services/prices.types.ts</read_first>
  <action>Update `pnpm-workspace.yaml` to include `packages/*`. Create a new workspace package `packages/contracts` with package name `@appstore-price-radar/contracts`, `type: "module"`, and typed exports from `src/index.ts`. Add `packages/contracts/src/auth.ts` with exact DTO names `AuthUserDto`, `AuthSessionDto`, `AuthResponseDto`, `AuthMeResponseDto`, `AuthOkResponseDto`, `AuthErrorDto`, and `SendLoginCodeResponseDto`. Add `packages/contracts/src/subscriptions.ts` with exact DTO names `SubscriptionItemDto`, `CreateSubscriptionResponseDto`, `ListSubscriptionsResponseDto`, `DeleteSubscriptionResponseDto`, and `SubscriptionErrorDto`. Add `packages/contracts/src/prices.ts` with exact DTO names `AppSnapshotDto`, `PriceChangeEventDto`, `PriceHistoryPageDto`, `PriceHistorySummaryDto`, and `PriceHistoryResponseDto`. Ensure `packages/contracts/src/index.ts` re-exports all DTO modules. If package resolution needs it, add workspace dependency entries from `apps/web/package.json` and `apps/worker/package.json` to `@appstore-price-radar/contracts: workspace:*`.</action>
  <verify>pnpm typecheck</verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains `packages/*`
    - `packages/contracts/package.json` contains `"name": "@appstore-price-radar/contracts"`
    - `packages/contracts/src/auth.ts` contains `export interface AuthResponseDto`
    - `packages/contracts/src/subscriptions.ts` contains `export interface SubscriptionItemDto`
    - `packages/contracts/src/prices.ts` contains `export interface PriceHistoryResponseDto`
    - `packages/contracts/src/index.ts` contains `export * from './auth'`
  </acceptance_criteria>
  <done>共享 contracts package 已建立，Phase 4 后续所有 Web/Worker 契约改动都有统一 source of truth。</done>
</task>

<task type="auto">
  <name>Task 2: 让 Worker 和 Web 都切到共享 DTO，并增加 response shape 回归断言</name>
  <files>apps/worker/src/routes/auth.ts, apps/worker/src/routes/subscriptions.ts, apps/worker/src/routes/prices.ts, apps/worker/src/services/auth.types.ts, apps/worker/src/services/subscriptions.types.ts, apps/worker/src/services/prices.types.ts, apps/worker/src/services/auth.ts, apps/worker/src/services/subscriptions.ts, apps/worker/src/services/prices.ts, apps/web/src/views/auth/types.ts, apps/web/src/views/profile/types.ts, apps/web/src/views/app/types.ts, apps/worker/test/auth.test.ts, apps/worker/test/subscriptions.create.test.ts</files>
  <read_first>apps/worker/src/routes/auth.ts, apps/worker/src/routes/subscriptions.ts, apps/worker/src/routes/prices.ts, apps/worker/src/services/auth.ts, apps/worker/src/services/subscriptions.ts, apps/worker/src/services/prices.ts, packages/contracts/src/auth.ts, packages/contracts/src/subscriptions.ts, packages/contracts/src/prices.ts, apps/web/src/views/auth/types.ts, apps/web/src/views/profile/types.ts, apps/web/src/views/app/types.ts, apps/worker/test/auth.test.ts, apps/worker/test/subscriptions.create.test.ts</read_first>
  <action>Update Worker auth / subscription / price services and routes so exported HTTP response types reference the shared DTO names from `@appstore-price-radar/contracts`, not ad-hoc inline objects. Add explicit mapper helpers named `toAuthUserDto`, `toSubscriptionItemDto`, `toAppSnapshotDto`, and `toPriceChangeEventDto` in the relevant service files or a local mapper section, converting `Date` fields to ISO strings and preserving nullable values exactly. Update `/api/auth/me` to return `AuthMeResponseDto`, `/api/subscriptions` list/create/delete to return shared subscription DTO shapes, and `/api/prices/:appId` to return `PriceHistoryResponseDto`. Replace the local Web type declarations in `apps/web/src/views/auth/types.ts`, `apps/web/src/views/profile/types.ts`, and `apps/web/src/views/app/types.ts` with re-exports/imports from `@appstore-price-radar/contracts`. Extend `apps/worker/test/auth.test.ts` and `apps/worker/test/subscriptions.create.test.ts` so they assert the JSON body keys and ISO string fields match the shared DTO contract exactly.</action>
  <verify>pnpm typecheck && pnpm --filter @appstore-price-radar/worker test -- test/auth.test.ts test/subscriptions.create.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/routes/auth.ts` contains `AuthMeResponseDto`
    - `apps/worker/src/routes/subscriptions.ts` contains `ListSubscriptionsResponseDto`
    - `apps/worker/src/routes/prices.ts` contains `PriceHistoryResponseDto`
    - `apps/worker/src/services/subscriptions.ts` contains `toSubscriptionItemDto`
    - `apps/worker/src/services/prices.ts` contains `toAppSnapshotDto`
    - `apps/web/src/views/auth/types.ts` contains `from '@appstore-price-radar/contracts'`
    - `apps/web/src/views/profile/types.ts` contains `SubscriptionItemDto`
    - `apps/web/src/views/app/types.ts` contains `PriceHistoryResponseDto`
  </acceptance_criteria>
  <done>前后端已经站在同一份 DTO 契约上，后续 401 统一处理和 history 分页改造不会再建立在漂移类型上。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/auth.test.ts test/subscriptions.create.test.ts`
- [ ] `rg -n "@appstore-price-radar/contracts|AuthResponseDto|SubscriptionItemDto|PriceHistoryResponseDto|toAuthUserDto|toSubscriptionItemDto|toAppSnapshotDto" package.json pnpm-workspace.yaml packages/contracts apps/web/src apps/worker/src`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/04-contract-frontend-stability/04-01-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- Auth / subscriptions / history DTO now have one shared source
- Worker routes return mapped DTO shapes instead of anonymous raw objects
- Web consumes the shared DTO package directly
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/04-contract-frontend-stability/04-01-SUMMARY.md`
</output>
