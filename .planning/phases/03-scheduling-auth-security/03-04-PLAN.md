---
phase: 03-scheduling-auth-security
plan: '04'
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - apps/worker/test/scheduler.job-lock.test.ts
  - apps/worker/test/jobs.check-route.test.ts
  - apps/worker/test/auth.security.test.ts
  - apps/worker/test/fresh-install.smoke.test.ts
autonomous: true
requirements:
  - PRICE-03
  - PRICE-04
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - 调度和认证的高风险边界都有自动化回归，不需要靠手工猜测系统是否安全。
    - 本 phase 的 5 个 requirement IDs 都能在测试文件中找到明确覆盖点。
  artifacts:
    - apps/worker/test/scheduler.job-lock.test.ts
    - apps/worker/test/jobs.check-route.test.ts
    - apps/worker/test/auth.security.test.ts
    - apps/worker/test/fresh-install.smoke.test.ts
  key_links:
    - `scheduler.job-lock.test.ts` 锁定 duplicate-run / summary 语义。
    - `jobs.check-route.test.ts` 锁定 manual route 门禁。
    - `auth.security.test.ts` 锁定限流、旧凭证淘汰和错误分支。
---

<objective>
补齐调度与认证的高风险边界测试，让 Phase 3 的安全能力有持续回归基线。

Purpose: 前三个 plan 会引入锁、summary、rate limit、route gate；如果没有统一的高风险回归，后续 phase 很容易把这些安全约束再次改坏。
Output: 覆盖 duplicate-run、手动 route 配置保护、auth 爆破限制、旧凭证淘汰和 smoke path 的测试套件补强。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-auth-security/03-RESEARCH.md
@.planning/phases/03-scheduling-auth-security/03-VALIDATION.md
@apps/worker/test/scheduler.job-lock.test.ts
@apps/worker/test/jobs.check-route.test.ts
@apps/worker/test/auth.security.test.ts
@apps/worker/test/fresh-install.smoke.test.ts
@apps/worker/src/services/jobs.ts
@apps/worker/src/services/auth.ts
@apps/worker/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补 scheduler 高风险边界测试，锁住 duplicate-run 与 run summary 契约</name>
  <files>apps/worker/test/scheduler.job-lock.test.ts, apps/worker/test/jobs.check-route.test.ts</files>
  <read_first>apps/worker/src/services/jobs.ts, apps/worker/src/index.ts, apps/worker/test/scheduler.rate-limit.test.ts, apps/worker/test/jobs.check-route.test.ts, apps/worker/test/fresh-install.smoke.test.ts</read_first>
  <action>Extend `apps/worker/test/scheduler.job-lock.test.ts` so it covers these exact behaviors: `marks one run as skipped when a second trigger arrives before the lease expires`, `persists completed run summaries with succeeded/skipped/failed counts`, and `captures failure reasons in errorSummary when refreshSingleApp throws`. Extend `apps/worker/test/jobs.check-route.test.ts` so a manual route call receiving a lock-held result returns a structured skip payload instead of a generic success body.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/scheduler.job-lock.test.ts test/jobs.check-route.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/scheduler.job-lock.test.ts` contains `marks one run as skipped when a second trigger arrives before the lease expires`
    - `apps/worker/test/scheduler.job-lock.test.ts` contains `persists completed run summaries with succeeded/skipped/failed counts`
    - `apps/worker/test/scheduler.job-lock.test.ts` contains `captures failure reasons in errorSummary when refreshSingleApp throws`
    - `apps/worker/test/jobs.check-route.test.ts` contains `returns a structured skip payload when a price-check lease is already active`
  </acceptance_criteria>
  <done>调度高风险边界已经有完整测试护栏，后续再改 orchestration 时不会轻易破坏互斥和 summary。</done>
</task>

<task type="auto">
  <name>Task 2: 补 auth 与 smoke 高风险边界测试，锁定限流和旧凭证淘汰</name>
  <files>apps/worker/test/auth.security.test.ts, apps/worker/test/fresh-install.smoke.test.ts</files>
  <read_first>apps/worker/src/services/auth.ts, apps/worker/src/routes/auth.ts, apps/worker/src/middleware/auth.ts, apps/worker/test/auth.security.test.ts, apps/worker/test/fresh-install.smoke.test.ts</read_first>
  <action>Extend `apps/worker/test/auth.security.test.ts` to cover `blocks repeated invalid reset token attempts`, `clears the login-password rate-limit scope after a successful login`, and `rejects an older login code after a newer code is issued`. Update `apps/worker/test/fresh-install.smoke.test.ts` so the authenticated/manual-check happy path still passes under `MANUAL_PRICE_CHECKS_ENABLED=true`, and add one negative route assertion showing the manual route rejects missing secret headers with `401`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/auth.security.test.ts test/fresh-install.smoke.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/auth.security.test.ts` contains `blocks repeated invalid reset token attempts`
    - `apps/worker/test/auth.security.test.ts` contains `clears the login-password rate-limit scope after a successful login`
    - `apps/worker/test/auth.security.test.ts` contains `rejects an older login code after a newer code is issued`
    - `apps/worker/test/fresh-install.smoke.test.ts` contains `missing secret`
    - `apps/worker/test/fresh-install.smoke.test.ts` contains `401`
  </acceptance_criteria>
  <done>auth 和 smoke 高风险边界也已被测试锁住，Phase 3 的 requirement coverage 达到可持续回归水平。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/scheduler.job-lock.test.ts test/jobs.check-route.test.ts test/auth.security.test.ts test/fresh-install.smoke.test.ts`
- [ ] `rg -n "succeeded/skipped/failed|price-check lease|older login code|invalid reset token|missing secret" apps/worker/test`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/03-scheduling-auth-security/03-04-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- 调度与认证高风险边界都有自动化覆盖
- Phase 3 requirement IDs 全部能映射到测试文件
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-auth-security/03-04-SUMMARY.md`
</output>
