---
phase: 03-scheduling-auth-security
plan: '03'
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - apps/worker/src/constants/env.ts
  - apps/worker/src/env.ts
  - apps/worker/src/types.ts
  - apps/worker/src/index.ts
  - apps/worker/src/services/jobs.ts
  - apps/worker/test/jobs.check-route.test.ts
  - apps/worker/test/fresh-install.smoke.test.ts
  - apps/worker/.dev.vars.example
  - README.md
autonomous: true
requirements:
  - AUTH-03
  - PRICE-04
user_setup: []
must_haves:
  truths:
    - `POST /api/jobs/check` 只有在显式开启且 `CRON_SECRET` 配置正确时才可访问。
    - 手动巡检和 scheduled 巡检共用 03-01 的 protected job service，因此同样能得到 run summary 和 duplicate-run skip 语义。
  artifacts:
    - apps/worker/src/index.ts
    - apps/worker/src/env.ts
    - apps/worker/src/services/jobs.ts
    - apps/worker/test/jobs.check-route.test.ts
    - README.md
  key_links:
    - `apps/worker/src/index.ts` 的 manual route 使用与 scheduled 相同的 `runProtectedPriceCheck(...)`。
    - `apps/worker/test/jobs.check-route.test.ts` 和 `apps/worker/test/fresh-install.smoke.test.ts` 同时锁定配置门禁与真实 route 行为。
---

<objective>
强化手动巡检入口的生产鉴权与配置保护，把当前“有 secret 就校验、没 secret 就放行”的危险默认值彻底收敛掉。

Purpose: `AUTH-03` 的重点是让手动巡检入口在配置不完整时直接不可用，并且即使被手动调用，也必须复用 Phase 3 已建立的互斥与统计逻辑。
Output: `MANUAL_PRICE_CHECKS_ENABLED` 开关、严格的 `CRON_SECRET` route guard、复用 protected job service 的 manual route，以及同步更新的文档与 smoke tests。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-auth-security/03-RESEARCH.md
@.planning/phases/03-scheduling-auth-security/03-VALIDATION.md
@apps/worker/src/index.ts
@apps/worker/src/services/jobs.ts
@apps/worker/src/env.ts
@apps/worker/src/constants/env.ts
@apps/worker/src/types.ts
@apps/worker/.dev.vars.example
@README.md
@apps/worker/test/fresh-install.smoke.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为手动巡检入口增加显式开关和严格 secret guard</name>
  <files>apps/worker/src/constants/env.ts, apps/worker/src/env.ts, apps/worker/src/types.ts, apps/worker/src/index.ts, apps/worker/src/services/jobs.ts, apps/worker/test/jobs.check-route.test.ts</files>
  <read_first>apps/worker/src/index.ts, apps/worker/src/services/jobs.ts, apps/worker/src/env.ts, apps/worker/src/constants/env.ts, apps/worker/src/types.ts, apps/worker/test/fresh-install.smoke.test.ts</read_first>
  <action>Extend `apps/worker/src/constants/env.ts`, `apps/worker/src/env.ts`, and `apps/worker/src/types.ts` with `MANUAL_PRICE_CHECKS_ENABLED=false`. Update `apps/worker/src/index.ts` so `POST /api/jobs/check` follows this exact policy: when `MANUAL_PRICE_CHECKS_ENABLED` is false, return `404 { error: 'Not Found' }`; when it is true but `CRON_SECRET` is missing, return `503 { error: 'CRON_SECRET is required when manual price checks are enabled' }`; when the request header `x-cron-secret` does not match, return `401 { error: 'Unauthorized' }`; otherwise call `runProtectedPriceCheck(config, { trigger: 'manual' })` and return its summary/skip payload. If `apps/worker/src/services/jobs.ts` needs a second trigger literal, add `manual` handling there instead of branching in the route. Create `apps/worker/test/jobs.check-route.test.ts` covering disabled, missing-secret, bad-secret, completed-manual-run, and duplicate-run-skipped cases.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/jobs.check-route.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/env.ts` contains `MANUAL_PRICE_CHECKS_ENABLED`
    - `apps/worker/src/index.ts` contains `CRON_SECRET is required when manual price checks are enabled`
    - `apps/worker/src/index.ts` contains `return c.json({ error: 'Not Found' }, 404)`
    - `apps/worker/src/index.ts` contains `trigger: 'manual'`
    - `apps/worker/test/jobs.check-route.test.ts` contains `returns 404 when manual price checks are disabled`
    - `apps/worker/test/jobs.check-route.test.ts` contains `returns 503 when manual checks are enabled without CRON_SECRET`
    - `apps/worker/test/jobs.check-route.test.ts` contains `returns 401 when x-cron-secret is invalid`
  </acceptance_criteria>
  <done>手动巡检入口已经有代码级门禁，不会再因为 secret 漏配而默认暴露到公网。</done>
</task>

<task type="auto">
  <name>Task 2: 更新本地示例、README 和 smoke 验证，使新门禁有一致的运维路径</name>
  <files>apps/worker/.dev.vars.example, README.md, apps/worker/test/fresh-install.smoke.test.ts</files>
  <read_first>apps/worker/.dev.vars.example, README.md, apps/worker/test/fresh-install.smoke.test.ts, apps/worker/src/index.ts</read_first>
  <action>Update `apps/worker/.dev.vars.example` to add `MANUAL_PRICE_CHECKS_ENABLED=false` alongside `CRON_SECRET`, and document that local manual route testing requires both values. Update `README.md` so the manual price-check curl example explicitly says `MANUAL_PRICE_CHECKS_ENABLED=true` and `CRON_SECRET` must both be set before using `POST /api/jobs/check`. Update `apps/worker/test/fresh-install.smoke.test.ts` bindings to include `MANUAL_PRICE_CHECKS_ENABLED: 'true'`, keep `CRON_SECRET: 'cron-secret-123'`, and assert the cron route still returns a structured report under the new guard.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/jobs.check-route.test.ts test/fresh-install.smoke.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/.dev.vars.example` contains `MANUAL_PRICE_CHECKS_ENABLED=false`
    - `README.md` contains `MANUAL_PRICE_CHECKS_ENABLED`
    - `README.md` contains `CRON_SECRET is required`
    - `apps/worker/test/fresh-install.smoke.test.ts` contains `MANUAL_PRICE_CHECKS_ENABLED`
    - `apps/worker/test/fresh-install.smoke.test.ts` contains `x-cron-secret`
  </acceptance_criteria>
  <done>文档、示例和 smoke path 都和新的手动巡检门禁保持一致，后续运维不会沿着旧路径误用。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/jobs.check-route.test.ts test/fresh-install.smoke.test.ts`
- [ ] `rg -n "MANUAL_PRICE_CHECKS_ENABLED|CRON_SECRET is required when manual price checks are enabled|trigger: 'manual'|x-cron-secret" apps/worker/src apps/worker/test README.md apps/worker/.dev.vars.example`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/03-scheduling-auth-security/03-03-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- 手动巡检入口只在显式开启且 secret 正确时可用
- manual 和 scheduled 共用同一套 protected job orchestration
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-auth-security/03-03-SUMMARY.md`
</output>
