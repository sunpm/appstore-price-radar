---
phase: 03-scheduling-auth-security
plan: '02'
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/worker/src/db/schema.ts
  - apps/worker/drizzle/0003_auth_security_limits.sql
  - apps/worker/src/constants/env.ts
  - apps/worker/src/env.ts
  - apps/worker/src/types.ts
  - apps/worker/src/services/auth-rate-limit.ts
  - apps/worker/src/services/auth.ts
  - apps/worker/src/services/auth.types.ts
  - apps/worker/test/auth.security.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - 密码登录、验证码发送/验证和密码重置都具备数据库支持的窗口限流，不再只有 resend cooldown。
    - 同一用户签发新的 login code 或 reset token 时，旧的未使用凭证会被显式淘汰。
  artifacts:
    - apps/worker/src/services/auth-rate-limit.ts
    - apps/worker/src/services/auth.ts
    - apps/worker/src/db/schema.ts
    - apps/worker/test/auth.security.test.ts
  key_links:
    - `apps/worker/src/services/auth.ts` 的 login/send-code/verify/reset 流程共用同一个 rate-limit helper。
    - `apps/worker/test/auth.security.test.ts` 同时覆盖限流和凭证淘汰，不让两个 requirement 再次漂移。
---

<objective>
给认证链路增加统一限流、失败计数和凭证淘汰策略，把当前“功能可用但没有护栏”的状态收敛到可验证安全基线。

Purpose: `AUTH-01` / `AUTH-02` 关心的不是 auth API 能不能工作，而是错误密码、错误验证码、频繁请求、重复签发凭证时系统是否会明确拒绝并回收旧凭证。
Output: 数据库支撑的 auth rate-limit state、`auth.ts` 中的统一安全规则、以及覆盖登录/验证码/重置流程的回归测试。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-auth-security/03-RESEARCH.md
@.planning/phases/03-scheduling-auth-security/03-VALIDATION.md
@apps/worker/src/services/auth.ts
@apps/worker/src/services/auth.types.ts
@apps/worker/src/routes/auth.ts
@apps/worker/src/middleware/auth.ts
@apps/worker/src/db/schema.ts
@apps/worker/src/env.ts
@apps/worker/src/constants/env.ts
@apps/worker/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 auth rate-limit schema、helper 和配置默认值</name>
  <files>apps/worker/src/db/schema.ts, apps/worker/drizzle/0003_auth_security_limits.sql, apps/worker/src/constants/env.ts, apps/worker/src/env.ts, apps/worker/src/types.ts, apps/worker/src/services/auth-rate-limit.ts</files>
  <read_first>apps/worker/src/db/schema.ts, apps/worker/drizzle/0000_init.sql, apps/worker/src/env.ts, apps/worker/src/constants/env.ts, apps/worker/src/types.ts, apps/worker/src/services/auth.ts</read_first>
  <action>Update `apps/worker/src/db/schema.ts` to add an `authRateLimits` table with `id`, `scope`, `subjectKey`, `attemptCount`, `windowStartedAt`, `blockedUntil`, and `updatedAt`, plus a unique index on `(scope, subjectKey)`. Create `apps/worker/drizzle/0003_auth_security_limits.sql` with matching SQL DDL. Extend `apps/worker/src/constants/env.ts`, `apps/worker/src/env.ts`, and `apps/worker/src/types.ts` with `AUTH_RATE_LIMIT_WINDOW_MINUTES=15`, `AUTH_RATE_LIMIT_MAX_ATTEMPTS=5`, and `AUTH_RATE_LIMIT_BLOCK_MINUTES=15`. Create `apps/worker/src/services/auth-rate-limit.ts` with helpers named `assertAuthRateLimit`, `recordAuthRateLimitFailure`, and `clearAuthRateLimit`, where scopes are concrete string literals `login-password`, `send-login-code`, `verify-login-code`, `forgot-password`, and `reset-password`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/auth.security.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/db/schema.ts` contains `export const authRateLimits`
    - `apps/worker/drizzle/0003_auth_security_limits.sql` contains `CREATE TABLE IF NOT EXISTS auth_rate_limits`
    - `apps/worker/src/env.ts` contains `AUTH_RATE_LIMIT_WINDOW_MINUTES`
    - `apps/worker/src/env.ts` contains `AUTH_RATE_LIMIT_MAX_ATTEMPTS`
    - `apps/worker/src/env.ts` contains `AUTH_RATE_LIMIT_BLOCK_MINUTES`
    - `apps/worker/src/services/auth-rate-limit.ts` contains `assertAuthRateLimit`
    - `apps/worker/src/services/auth-rate-limit.ts` contains `recordAuthRateLimitFailure`
    - `apps/worker/src/services/auth-rate-limit.ts` contains `clearAuthRateLimit`
  </acceptance_criteria>
  <done>认证限流已经有稳定的数据库状态表、helper 和配置入口，后续 auth flow 可以统一复用。</done>
</task>

<task type="auto">
  <name>Task 2: 把限流和旧凭证淘汰规则接入 auth 主流程</name>
  <files>apps/worker/src/services/auth.ts, apps/worker/src/services/auth.types.ts</files>
  <read_first>apps/worker/src/services/auth.ts, apps/worker/src/services/auth.types.ts, apps/worker/src/services/auth-rate-limit.ts, apps/worker/src/db/schema.ts, apps/worker/src/routes/auth.ts</read_first>
  <action>Update `apps/worker/src/services/auth.ts` so `loginWithPassword()` calls `assertAuthRateLimit(... scope: 'login-password' ...)` before password verification, records failures when user missing or password mismatch, and clears the scope on success. Apply the same pattern to `sendLoginCode()` with scope `send-login-code`, `verifyLoginCode()` and `registerWithLoginCode()` with scope `verify-login-code`, `requestPasswordReset()` with scope `forgot-password`, and `resetPassword()` with scope `reset-password`. Before inserting a new login code, explicitly invalidate previous active login codes for the same user by setting `usedAt = now`; before inserting a new password reset token, explicitly invalidate previous active reset tokens for the same user. Update `apps/worker/src/services/auth.types.ts` so 429 responses continue to use `retryAfterSeconds` and do not require a new response shape.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/auth.security.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/services/auth.ts` contains `login-password`
    - `apps/worker/src/services/auth.ts` contains `send-login-code`
    - `apps/worker/src/services/auth.ts` contains `verify-login-code`
    - `apps/worker/src/services/auth.ts` contains `forgot-password`
    - `apps/worker/src/services/auth.ts` contains `reset-password`
    - `apps/worker/src/services/auth.ts` contains `update(loginCodes)`
    - `apps/worker/src/services/auth.ts` contains `update(passwordResetTokens)`
    - `apps/worker/src/services/auth.types.ts` contains `retryAfterSeconds?: number`
  </acceptance_criteria>
  <done>auth 核心流程已经统一走限流和旧凭证淘汰规则，风险不再只靠单点 cooldown 托底。</done>
</task>

<task type="auto">
  <name>Task 3: 为 auth 安全护栏补确定性的回归测试</name>
  <files>apps/worker/test/auth.security.test.ts</files>
  <read_first>apps/worker/src/services/auth.ts, apps/worker/src/services/auth-rate-limit.ts, apps/worker/src/db/schema.ts, apps/worker/test/auth.test.ts, apps/worker/test/fresh-install.smoke.test.ts</read_first>
  <action>Create `apps/worker/test/auth.security.test.ts` with deterministic DB mocks covering at least these cases: `blocks password login after repeated invalid credentials`, `revokes older login codes when issuing a new code`, `revokes older password reset tokens when issuing a new token`, and `blocks repeated invalid code verification attempts`. Use explicit scope keys and assert the returned status codes are `429`, `401`, or `200` as expected, plus inspect the in-memory DB state so only the newest active credential remains usable.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/auth.security.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/auth.security.test.ts` contains `blocks password login after repeated invalid credentials`
    - `apps/worker/test/auth.security.test.ts` contains `revokes older login codes when issuing a new code`
    - `apps/worker/test/auth.security.test.ts` contains `revokes older password reset tokens when issuing a new token`
    - `apps/worker/test/auth.security.test.ts` contains `blocks repeated invalid code verification attempts`
  </acceptance_criteria>
  <done>AUTH-01 和 AUTH-02 都有可复跑、可审计的回归测试，不再依赖人工猜测。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/auth.security.test.ts`
- [ ] `rg -n "authRateLimits|AUTH_RATE_LIMIT_WINDOW_MINUTES|AUTH_RATE_LIMIT_MAX_ATTEMPTS|AUTH_RATE_LIMIT_BLOCK_MINUTES|login-password|send-login-code|verify-login-code|forgot-password|reset-password" apps/worker/src apps/worker/drizzle apps/worker/test`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/03-scheduling-auth-security/03-02-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- auth flows 具备统一限流与失败计数
- 新凭证会淘汰旧的 login code / reset token
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-auth-security/03-02-SUMMARY.md`
</output>
