---
phase: 03-scheduling-auth-security
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/db/schema.ts
  - apps/worker/drizzle/0002_job_locks_price_check_runs.sql
  - apps/worker/src/lib/checker.ts
  - apps/worker/src/lib/checker.types.ts
  - apps/worker/src/env.ts
  - apps/worker/src/constants/env.ts
  - apps/worker/src/types.ts
  - apps/worker/src/services/jobs.ts
  - apps/worker/src/index.ts
  - apps/worker/test/scheduler.job-lock.test.ts
  - apps/worker/test/scheduler.rate-limit.test.ts
autonomous: true
requirements:
  - PRICE-03
  - PRICE-04
user_setup: []
must_haves:
  truths:
    - 同一时刻只能有一个 `price-check` 任务真正执行，重复触发会返回结构化 skip 结果而不是再次跑完全流程。
    - 一轮巡检会输出并持久化 `scanned`、`succeeded`、`skipped`、`failed`、`updated`、`drops`、`emailsSent` 和失败原因摘要。
  artifacts:
    - apps/worker/src/services/jobs.ts
    - apps/worker/src/lib/checker.ts
    - apps/worker/src/lib/checker.types.ts
    - apps/worker/src/db/schema.ts
    - apps/worker/test/scheduler.job-lock.test.ts
  key_links:
    - `apps/worker/src/index.ts` 的 `scheduled()` 入口与新的 job orchestration service 使用同一套租约逻辑。
    - `apps/worker/src/lib/checker.types.ts` 的 report 字段与 `price_check_runs` 的 summary 字段保持一一对应。
---

<objective>
为价格巡检增加 run-level 互斥和任务级统计，先把“能不能跑”和“跑完发生了什么”稳定下来。

Purpose: `PRICE-03` / `PRICE-04` 的关键不是继续优化每个 app 的刷新细节，而是先防止整轮巡检重复执行，并把本轮成功/跳过/失败原因做成可以测试、可以持久化的明确契约。
Output: 数据库租约 + run summary schema、新的 jobs orchestration service、扩展后的 `CheckReport` 与对应回归测试。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-auth-security/03-RESEARCH.md
@.planning/phases/03-scheduling-auth-security/03-VALIDATION.md
@apps/worker/src/index.ts
@apps/worker/src/lib/checker.ts
@apps/worker/src/lib/checker.types.ts
@apps/worker/src/db/schema.ts
@apps/worker/src/env.ts
@apps/worker/src/constants/env.ts
@apps/worker/src/types.ts
@apps/worker/test/scheduler.rate-limit.test.ts
@apps/worker/test/fresh-install.smoke.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加巡检租约和运行摘要 schema，并把配置补齐到 env/types</name>
  <files>apps/worker/src/db/schema.ts, apps/worker/drizzle/0002_job_locks_price_check_runs.sql, apps/worker/src/env.ts, apps/worker/src/constants/env.ts, apps/worker/src/types.ts, apps/worker/src/lib/checker.types.ts</files>
  <read_first>apps/worker/src/db/schema.ts, apps/worker/drizzle/0001_price_change_events.sql, apps/worker/src/env.ts, apps/worker/src/constants/env.ts, apps/worker/src/types.ts, apps/worker/src/lib/checker.types.ts</read_first>
  <action>Update `apps/worker/src/db/schema.ts` to add a `jobLeases` table with `lockKey`, `runId`, `lockedUntil`, `updatedAt`, and a `priceCheckRuns` table with `id`, `trigger`, `status`, `startedAt`, `finishedAt`, `scanned`, `succeeded`, `skipped`, `failed`, `updated`, `drops`, `emailsSent`, and `errorSummary`. Create `apps/worker/drizzle/0002_job_locks_price_check_runs.sql` with matching SQL DDL, including a primary key on `job_leases.lock_key` and indexes that support querying recent `price_check_runs`. Extend `apps/worker/src/constants/env.ts`, `apps/worker/src/env.ts`, and `apps/worker/src/types.ts` with `PRICE_CHECK_LOCK_TTL_SECONDS` defaulting to `900`. Update `apps/worker/src/lib/checker.types.ts` so `CheckReport` contains exact fields `succeeded`, `skipped`, and `failed` in addition to existing `scanned`, `updated`, `drops`, `emailsSent`, and `errors`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/scheduler.job-lock.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/db/schema.ts` contains `export const jobLeases`
    - `apps/worker/src/db/schema.ts` contains `export const priceCheckRuns`
    - `apps/worker/drizzle/0002_job_locks_price_check_runs.sql` contains `CREATE TABLE IF NOT EXISTS job_leases`
    - `apps/worker/drizzle/0002_job_locks_price_check_runs.sql` contains `CREATE TABLE IF NOT EXISTS price_check_runs`
    - `apps/worker/src/env.ts` contains `PRICE_CHECK_LOCK_TTL_SECONDS`
    - `apps/worker/src/lib/checker.types.ts` contains `succeeded: number`
    - `apps/worker/src/lib/checker.types.ts` contains `skipped: number`
    - `apps/worker/src/lib/checker.types.ts` contains `failed: number`
  </acceptance_criteria>
  <done>巡检互斥和运行摘要已经有稳定 schema 与配置入口，后续 service 可以直接依赖这些 source of truth。</done>
</task>

<task type="auto">
  <name>Task 2: 抽出受保护的巡检 orchestration service，并让 scheduled 入口使用它</name>
  <files>apps/worker/src/services/jobs.ts, apps/worker/src/lib/checker.ts, apps/worker/src/index.ts, apps/worker/test/scheduler.job-lock.test.ts, apps/worker/test/scheduler.rate-limit.test.ts</files>
  <read_first>apps/worker/src/index.ts, apps/worker/src/lib/checker.ts, apps/worker/src/lib/checker.types.ts, apps/worker/src/db/schema.ts, apps/worker/test/scheduler.rate-limit.test.ts, apps/worker/test/fresh-install.smoke.test.ts</read_first>
  <action>Create `apps/worker/src/services/jobs.ts` with a `runProtectedPriceCheck(config, { trigger })` helper that (1) acquires the `price-check` lease using `PRICE_CHECK_LOCK_TTL_SECONDS`, (2) returns `{ kind: 'skipped', reason: 'price-check-already-running' }` when an active lease exists, (3) inserts a `priceCheckRuns` row with `status: 'running'`, (4) calls `runPriceCheck(config)`, (5) persists the final summary counts and `errorSummary`, and (6) releases or refreshes the lease on completion/failure. Update `apps/worker/src/lib/checker.ts` so `CheckReport` increments `succeeded`, `skipped`, and `failed` deterministically: `null` lookup results count as `skipped`, successful refreshes count as `succeeded`, and caught exceptions count as `failed` while still appending to `errors`. Update `apps/worker/src/index.ts` so `scheduled()` uses `runProtectedPriceCheck(config, { trigger: 'scheduled' })` instead of calling `runPriceCheck(config)` directly, and logs either the completed summary or the skip reason. Add `apps/worker/test/scheduler.job-lock.test.ts` covering one completed run and one duplicate-run skip, and extend `apps/worker/test/scheduler.rate-limit.test.ts` to assert the returned report includes `succeeded`, `skipped`, and `failed` counters.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/scheduler.job-lock.test.ts test/scheduler.rate-limit.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/services/jobs.ts` contains `runProtectedPriceCheck`
    - `apps/worker/src/services/jobs.ts` contains `price-check-already-running`
    - `apps/worker/src/lib/checker.ts` contains `succeeded:`
    - `apps/worker/src/lib/checker.ts` contains `skipped:`
    - `apps/worker/src/lib/checker.ts` contains `failed:`
    - `apps/worker/src/index.ts` contains `trigger: 'scheduled'`
    - `apps/worker/test/scheduler.job-lock.test.ts` contains `skips duplicate price-check runs while a lease is active`
    - `apps/worker/test/scheduler.rate-limit.test.ts` contains `succeeded`
  </acceptance_criteria>
  <done>scheduled 巡检已经通过统一的 protected service 执行，重复运行会被显式跳过，summary 也具备可回归验证的结构。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/scheduler.job-lock.test.ts test/scheduler.rate-limit.test.ts`
- [ ] `rg -n "jobLeases|priceCheckRuns|PRICE_CHECK_LOCK_TTL_SECONDS|runProtectedPriceCheck|price-check-already-running|succeeded|skipped|failed" apps/worker/src apps/worker/drizzle apps/worker/test`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/03-scheduling-auth-security/03-01-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- 重复巡检不会再次进入完整执行路径
- run summary 具备成功/跳过/失败统计和失败原因摘要
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-auth-security/03-01-SUMMARY.md`
</output>
