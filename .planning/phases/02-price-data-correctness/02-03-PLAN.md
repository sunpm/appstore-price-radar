---
phase: 02-price-data-correctness
plan: '03'
type: execute
wave: 3
depends_on:
  - '02-01'
  - '02-02'
files_modified:
  - apps/worker/src/lib/checker.ts
  - apps/worker/src/lib/checker.types.ts
  - apps/worker/src/services/subscriptions.ts
  - apps/worker/test/subscriptions.create.test.ts
  - apps/worker/test/scheduler.rate-limit.test.ts
autonomous: true
requirements:
  - PRICE-05
user_setup: []
must_haves:
  truths:
    - 订阅创建后的即时刷新和定时巡检通过同一个 refresh options contract 进入共享规则路径。
    - 两条路径唯一允许的行为差异是 `notifyDrops`、`source` 和 `requestId` 等显式策略字段。
  artifacts:
    - apps/worker/src/lib/checker.ts
    - apps/worker/src/lib/checker.types.ts
    - apps/worker/src/services/subscriptions.ts
    - apps/worker/test/subscriptions.create.test.ts
    - apps/worker/test/scheduler.rate-limit.test.ts
  key_links:
    - apps/worker/src/services/subscriptions.ts 和 apps/worker/src/lib/checker.ts 共享同一个 `buildRefreshOptions(...)` 契约。
    - apps/worker/test/subscriptions.create.test.ts 与 apps/worker/test/scheduler.rate-limit.test.ts 共同锁定即时刷新和巡检的 options 差异。
---

<objective>
统一即时刷新与巡检的入口契约，防止 Phase 2 新规则只在一个入口生效。

Purpose: 当前两个入口虽然都能走到 `refreshSingleApp()`，但 options 是分散内联的。`PRICE-05` 需要把这些策略字段集中起来，让“共享规则”不仅体现在函数复用上，也体现在传参契约和测试上。
Output: 共享的 `buildRefreshOptions(...)` helper、订阅创建入口的显式 requestId/source 策略，以及对应的参数级回归测试。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-price-data-correctness/02-RESEARCH.md
@.planning/phases/02-price-data-correctness/02-VALIDATION.md
@apps/worker/src/lib/checker.ts
@apps/worker/src/lib/checker.types.ts
@apps/worker/src/services/subscriptions.ts
@apps/worker/test/scheduler.rate-limit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提取共享 refresh options contract</name>
  <files>apps/worker/src/lib/checker.ts, apps/worker/src/lib/checker.types.ts</files>
  <read_first>apps/worker/src/lib/checker.ts, apps/worker/src/lib/checker.types.ts, apps/worker/src/services/subscriptions.ts, .planning/phases/02-price-data-correctness/02-RESEARCH.md</read_first>
  <action>Update `apps/worker/src/lib/checker.types.ts` to introduce `type RefreshTrigger = 'subscription-create' | 'scheduled'` and a small execution payload that carries `trigger` and `requestId`. In `apps/worker/src/lib/checker.ts`, add a helper named `buildRefreshOptions` that maps `subscription-create` to `{ notifyDrops: false, source: 'manual', requestId }` and maps `scheduled` to `{ notifyDrops: true, source: 'scheduled', requestId }`. Replace the inline scheduled options object inside `runPriceCheck()` with this helper so future refresh rules only need one source of truth.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/scheduler.rate-limit.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/lib/checker.types.ts` contains `type RefreshTrigger = 'subscription-create' | 'scheduled'`
    - `apps/worker/src/lib/checker.ts` contains `const buildRefreshOptions =`
    - `apps/worker/src/lib/checker.ts` contains `trigger === 'scheduled'`
    - `apps/worker/src/lib/checker.ts` contains `source: 'manual'`
    - `apps/worker/src/lib/checker.ts` contains `source: 'scheduled'`
  </acceptance_criteria>
  <done>refresh options 不再散落在不同入口里，后续规则调整有了单一修改点。</done>
</task>

<task type="auto">
  <name>Task 2: 让订阅创建入口复用共享 contract，并补参数级回归测试</name>
  <files>apps/worker/src/services/subscriptions.ts, apps/worker/test/subscriptions.create.test.ts, apps/worker/test/scheduler.rate-limit.test.ts</files>
  <read_first>apps/worker/src/services/subscriptions.ts, apps/worker/src/lib/checker.ts, apps/worker/src/lib/checker.types.ts, apps/worker/test/scheduler.rate-limit.test.ts</read_first>
  <action>Update `apps/worker/src/services/subscriptions.ts` so the post-insert refresh no longer passes a handwritten `{ notifyDrops: false }` object. Instead, call the shared `buildRefreshOptions(...)` helper with `trigger: 'subscription-create'` and a deterministic `requestId` prefixed with `subscription-create:` using the returned subscription id. Create `apps/worker/test/subscriptions.create.test.ts` to mock `refreshSingleApp()` and assert `createUserSubscription()` passes `notifyDrops: false`, `source: 'manual'`, and a `requestId` starting with `subscription-create:`. Extend `apps/worker/test/scheduler.rate-limit.test.ts` to assert scheduled refreshes still call the shared contract with `notifyDrops: true`, `source: 'scheduled'`, and a `requestId` starting with `scheduled:`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/subscriptions.create.test.ts test/scheduler.rate-limit.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/services/subscriptions.ts` contains `subscription-create:`
    - `apps/worker/test/subscriptions.create.test.ts` contains `describe('createUserSubscription refresh contract'`
    - `apps/worker/test/subscriptions.create.test.ts` contains `source: 'manual'`
    - `apps/worker/test/subscriptions.create.test.ts` contains `notifyDrops: false`
    - `apps/worker/test/scheduler.rate-limit.test.ts` contains `source: 'scheduled'`
    - `apps/worker/test/scheduler.rate-limit.test.ts` contains `notifyDrops: true`
  </acceptance_criteria>
  <done>即时刷新和巡检入口都已显式复用同一个 options contract，只在提醒策略和 request metadata 上保留差异。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/scheduler.rate-limit.test.ts`
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/subscriptions.create.test.ts test/scheduler.rate-limit.test.ts`
- [ ] `rg -n "buildRefreshOptions|subscription-create:|source: 'manual'|source: 'scheduled'|notifyDrops: false|notifyDrops: true" apps/worker/src/lib/checker.ts apps/worker/src/lib/checker.types.ts apps/worker/src/services/subscriptions.ts apps/worker/test/subscriptions.create.test.ts apps/worker/test/scheduler.rate-limit.test.ts`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-price-data-correctness/02-03-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- 即时刷新与巡检通过同一个 refresh options contract 进入共享规则路径
- 两条路径只在提醒能力和 request metadata 上存在显式差异
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/02-price-data-correctness/02-03-SUMMARY.md`
</output>
