---
phase: 02-price-data-correctness
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/lib/appstore.ts
  - apps/worker/src/lib/appstore.types.ts
  - apps/worker/src/lib/checker.ts
  - apps/worker/test/appstore.lookup.test.ts
  - apps/worker/test/checker.price-change.test.ts
autonomous: true
requirements:
  - PRICE-01
user_setup: []
must_haves:
  truths:
    - 缺失或异常 App Store 价格不会再被回落成 `0` 元。
    - 无效价格分支不会更新 snapshot、change event 或 drop event。
  artifacts:
    - apps/worker/src/lib/appstore.ts
    - apps/worker/src/lib/appstore.types.ts
    - apps/worker/test/appstore.lookup.test.ts
    - apps/worker/test/checker.price-change.test.ts
  key_links:
    - apps/worker/src/lib/appstore.ts 和 apps/worker/src/lib/checker.ts 使用同一个 `invalid-price` 跳过契约。
    - apps/worker/test/appstore.lookup.test.ts 与 apps/worker/test/checker.price-change.test.ts 分别锁定解析层和持久化层的无效价格分支。
---

<objective>
修正 App Store lookup 边界，让“价格无效”不再被误写成真实业务值。

Purpose: `PRICE-01` 的核心不是“让更多请求成功”，而是“只在拿到可信价格时才继续写库”。这一步先把 lookup contract 和 checker 的 early-return 分支做扎实，给后续一致性改造提供干净输入。
Output: 明确的 `found / invalid-price / not-found` lookup contract，以及覆盖无效价格分支的回归测试。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-price-data-correctness/02-RESEARCH.md
@.planning/phases/02-price-data-correctness/02-VALIDATION.md
@apps/worker/src/lib/appstore.ts
@apps/worker/src/lib/appstore.types.ts
@apps/worker/src/lib/checker.ts
@apps/worker/test/checker.price-change.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 App Store lookup 返回值改成显式的 invalid-price 契约</name>
  <files>apps/worker/src/lib/appstore.ts, apps/worker/src/lib/appstore.types.ts, apps/worker/test/appstore.lookup.test.ts</files>
  <read_first>apps/worker/src/lib/appstore.ts, apps/worker/src/lib/appstore.types.ts, apps/worker/src/lib/checker.ts, apps/worker/test/checker.price-change.test.ts</read_first>
  <action>Update `apps/worker/src/lib/appstore.types.ts` so the lookup result is a discriminated union with `kind: 'found'` and `kind: 'invalid-price'`. Keep validated payloads under `kind: 'found'` with `price: number`, and add an invalid branch with `reason: 'missing-price'`, `appId`, `country`, `appName`, `currency`, `storeUrl`, `iconUrl`, and `formattedPrice`. Then update `apps/worker/src/lib/appstore.ts` so `fetchAppStorePrice(appId, country)` returns `null` only when `resultCount === 0`, returns `kind: 'invalid-price'` when `item.price` is `null`, `undefined`, or `NaN`, and never uses the fallback expression `price: typeof item.price === 'number' ? item.price : 0`. Create `apps/worker/test/appstore.lookup.test.ts` with a `describe('fetchAppStorePrice'` suite that covers one valid lookup and one “missing numeric price” lookup returning `kind: 'invalid-price'`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/appstore.lookup.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/lib/appstore.types.ts` contains `kind: 'invalid-price'`
    - `apps/worker/src/lib/appstore.types.ts` contains `reason: 'missing-price'`
    - `apps/worker/src/lib/appstore.ts` contains `kind: 'found'`
    - `apps/worker/src/lib/appstore.ts` contains `kind: 'invalid-price'`
    - `apps/worker/test/appstore.lookup.test.ts` contains `describe('fetchAppStorePrice'`
    - `apps/worker/test/appstore.lookup.test.ts` contains `returns invalid-price when App Store omits numeric price`
  </acceptance_criteria>
  <done>Lookup 层已经能区分真实价格、无效价格和 not-found，不再把缺失价格伪装成 `0`。</done>
</task>

<task type="auto">
  <name>Task 2: 在 checker 中提前跳过 invalid-price，禁止任何持久化写入</name>
  <files>apps/worker/src/lib/checker.ts, apps/worker/test/checker.price-change.test.ts</files>
  <read_first>apps/worker/src/lib/checker.ts, apps/worker/src/lib/appstore.ts, apps/worker/src/lib/appstore.types.ts, apps/worker/test/checker.price-change.test.ts</read_first>
  <action>Update `apps/worker/src/lib/checker.ts` so `refreshSingleApp()` handles the new lookup contract before creating a DB client: keep `null` for not-found, add an `if (data.kind === 'invalid-price')` branch, emit `console.warn('skipping invalid App Store price', { appId, country, reason: data.reason, formattedPrice: data.formattedPrice ?? null })`, and return `null` before any snapshot / event / drop-event write runs. Extend `apps/worker/test/checker.price-change.test.ts` with a case named `skips snapshot and history writes when App Store price is invalid` that mocks the invalid-price branch and asserts `dbState.snapshot`, `dbState.events`, and `dbState.dropEvents` stay unchanged.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/checker.price-change.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/lib/checker.ts` contains `if (data.kind === 'invalid-price')`
    - `apps/worker/src/lib/checker.ts` contains `console.warn('skipping invalid App Store price'`
    - `apps/worker/test/checker.price-change.test.ts` contains `skips snapshot and history writes when App Store price is invalid`
  </acceptance_criteria>
  <done>无效价格在进入持久化层之前就被阻断，不会再污染 snapshot 或历史事件。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/appstore.lookup.test.ts`
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/checker.price-change.test.ts`
- [ ] `rg -n "invalid-price|missing-price|skipping invalid App Store price" apps/worker/src/lib/appstore.ts apps/worker/src/lib/appstore.types.ts apps/worker/src/lib/checker.ts apps/worker/test/appstore.lookup.test.ts apps/worker/test/checker.price-change.test.ts`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-price-data-correctness/02-01-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- 无效价格不再被写成 `0`
- `refreshSingleApp()` 对 invalid-price 分支零持久化写入
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/02-price-data-correctness/02-01-SUMMARY.md`
</output>
