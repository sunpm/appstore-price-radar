---
phase: 02-price-data-correctness
plan: '02'
type: execute
wave: 2
depends_on:
  - '02-01'
files_modified:
  - apps/worker/src/lib/checker.ts
  - apps/worker/src/lib/checker.types.ts
  - apps/worker/test/checker.atomicity.test.ts
  - apps/worker/test/checker.price-change.test.ts
autonomous: true
requirements:
  - PRICE-02
user_setup: []
must_haves:
  truths:
    - 单次 `refreshSingleApp` 的 snapshot、change event 和 drop event 写入通过同一个 batched persistence 边界提交。
    - 持久化失败时不会留下只更新一部分表的中间态。
  artifacts:
    - apps/worker/src/lib/checker.ts
    - apps/worker/src/lib/checker.types.ts
    - apps/worker/test/checker.atomicity.test.ts
  key_links:
    - apps/worker/src/lib/checker.ts 使用 `db.batch(...)` 收敛单刷新写入边界。
    - apps/worker/test/checker.atomicity.test.ts 证明持久化失败时 snapshot / change event / drop event 不会部分落库。
---

<objective>
给单应用刷新增加最小但真实的一致性边界，先解决“半完成写入”问题。

Purpose: `PRICE-02` 在 Phase 2 里优先收敛的是“一个 refresh 内部不要自己把数据库写坏”。由于当前 `neon-http` driver 不支持 `db.transaction()`，这一步要明确采用当前栈可行的 batched persistence 路线，而不是写成无法执行的空泛建议。
Output: 使用 `db.batch(...)` 的 refresh persistence 重构，以及覆盖 batched failure / alert status 顺序的回归测试。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-price-data-correctness/02-RESEARCH.md
@.planning/phases/02-price-data-correctness/02-VALIDATION.md
@apps/worker/src/lib/checker.ts
@apps/worker/src/lib/checker.types.ts
@apps/worker/src/db/client.ts
@apps/worker/src/db/schema.ts
@apps/worker/test/checker.price-change.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 refresh 的数据库写入改成 batched persistence</name>
  <files>apps/worker/src/lib/checker.ts, apps/worker/src/lib/checker.types.ts</files>
  <read_first>apps/worker/src/lib/checker.ts, apps/worker/src/lib/checker.types.ts, apps/worker/src/db/client.ts, apps/worker/src/db/schema.ts, .planning/phases/02-price-data-correctness/02-RESEARCH.md</read_first>
  <action>Refactor `apps/worker/src/lib/checker.ts` so `refreshSingleApp()` separates decision-making from persistence. After computing `oldPrice`, `priceChanged`, `priceDropped`, `source`, and `requestId`, build a `writes` array of Drizzle queries where the snapshot upsert is always first, the `app_price_change_events` insert is appended only when `priceChanged && oldPrice !== undefined`, and the `app_drop_events` insert is appended only when `priceDropped && oldPrice !== undefined`. Execute those writes with one `await db.batch(writes)` call so the current `drizzle-orm/neon-http` driver submits them through the installed Neon HTTP transaction path. Do not call `db.transaction()`. Do not query alert targets or send email until after `db.batch(writes)` succeeds. Update `apps/worker/src/lib/checker.types.ts` as needed so the refresh result can still report `priceChanged`, `priceDropped`, and `alertsSent` after the new persistence flow.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/checker.price-change.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/lib/checker.ts` contains `const writes = [`
    - `apps/worker/src/lib/checker.ts` contains `await db.batch(writes)`
    - `apps/worker/src/lib/checker.ts` contains `if (priceChanged && oldPrice !== undefined)`
    - `apps/worker/src/lib/checker.ts` contains `if (priceDropped && oldPrice !== undefined)`
    - `apps/worker/src/lib/checker.ts` does not contain `db.transaction(`
  </acceptance_criteria>
  <done>单刷新里的核心数据库写入已经收敛到一个 batched persistence 边界，不再是多段独立提交。</done>
</task>

<task type="auto">
  <name>Task 2: 补持久化原子性和提醒状态顺序测试</name>
  <files>apps/worker/test/checker.atomicity.test.ts, apps/worker/test/checker.price-change.test.ts</files>
  <read_first>apps/worker/test/checker.price-change.test.ts, apps/worker/src/lib/checker.ts, apps/worker/src/db/schema.ts, apps/worker/src/services/subscriptions.ts</read_first>
  <action>Create `apps/worker/test/checker.atomicity.test.ts` with an in-memory DB mock that implements `batch()` and can force the batch to fail before commit. Add a test named `does not persist snapshot or events when batched writes fail` that verifies snapshot, change events, and drop events all remain unchanged after a thrown batch error. Add a second test named `does not change lastNotifiedPrice when alert delivery is not sent` that mocks `sendDropAlertEmail()` to return `{ sent: false }` and verifies the affected subscription record keeps its previous `lastNotifiedPrice`. Update `apps/worker/test/checker.price-change.test.ts` only as needed to stay compatible with the new batched write path.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/checker.atomicity.test.ts test/checker.price-change.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/checker.atomicity.test.ts` contains `describe('refreshSingleApp atomicity'`
    - `apps/worker/test/checker.atomicity.test.ts` contains `does not persist snapshot or events when batched writes fail`
    - `apps/worker/test/checker.atomicity.test.ts` contains `does not change lastNotifiedPrice when alert delivery is not sent`
  </acceptance_criteria>
  <done>Phase 2 拥有可以证明“批量写入失败不会留脏状态”的回归测试，而不是只靠代码注释声明一致性。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/checker.atomicity.test.ts`
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/checker.atomicity.test.ts test/checker.price-change.test.ts`
- [ ] `rg -n "db.batch\\(writes\\)|refreshSingleApp atomicity|batched writes fail|lastNotifiedPrice" apps/worker/src/lib/checker.ts apps/worker/test/checker.atomicity.test.ts`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-price-data-correctness/02-02-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- 单刷新数据库写入具备明确的 batched persistence 边界
- 持久化失败不会留下部分写入
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/02-price-data-correctness/02-02-SUMMARY.md`
</output>
