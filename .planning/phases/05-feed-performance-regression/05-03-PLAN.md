---
phase: 05-feed-performance-regression
plan: '03'
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - package.json
  - pnpm-lock.yaml
  - README.md
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/test/setup.ts
  - apps/web/test/auth-session.test.ts
  - apps/web/test/profile-subscription.test.ts
  - apps/web/test/app-history.test.ts
  - apps/web/src/router.ts
  - apps/web/src/lib/auth-session.ts
  - apps/web/src/composables/useAuthSession.ts
  - apps/web/src/composables/useAuthedApi.ts
  - apps/web/src/composables/usePriceHistory.ts
autonomous: true
requirements:
  - QA-02
  - QA-03
user_setup: []
must_haves:
  truths:
    - 前端自动化保护必须直接覆盖“登录态恢复/失效处理、订阅流、价格历史查看”三条关键路径，而不是停留在零散 util 测试。
    - 发布前统一验证命令必须真的把 Web 与 Worker 的类型检查、lint、自动化测试和 smoke path 串起来，不能只是新增一个空壳脚本名。
  artifacts:
    - apps/web/vitest.config.ts
    - apps/web/test/auth-session.test.ts
    - apps/web/test/profile-subscription.test.ts
    - apps/web/test/app-history.test.ts
    - package.json
  key_links:
    - `apps/web/src/router.ts`、`apps/web/src/composables/useAuthSession.ts` 与 `apps/web/src/composables/useAuthedApi.ts` 共同决定登录态恢复与未授权跳转路径，是 QA-02 的核心边界。
    - `apps/web/src/views/profile/ProfileView.vue` 与 `apps/web/src/composables/usePriceHistory.ts` 共同决定订阅流与历史数据加载行为，测试必须直接覆盖它们的交互。
---

<objective>
为前端建立最小可行的关键路径自动化测试基线，并在仓库根目录提供统一验证命令，让部署前验证从“分散命令集合”升级为“单一官方入口”。

Purpose: `QA-02` 与 `QA-03` 是一条链，前者提供前端自动化保护，后者把这些保护真正串进发布前流程。
Output: Web 测试基建、3 个关键路径测试文件、根 `test`/`verify` 命令与对应说明。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-feed-performance-regression/05-RESEARCH.md
@.planning/phases/05-feed-performance-regression/05-VALIDATION.md
@package.json
@README.md
@apps/web/package.json
@apps/web/src/router.ts
@apps/web/src/lib/auth-session.ts
@apps/web/src/composables/useAuthSession.ts
@apps/web/src/composables/useAuthedApi.ts
@apps/web/src/composables/usePriceHistory.ts
@apps/web/src/views/auth/AuthView.vue
@apps/web/src/views/profile/ProfileView.vue
@apps/web/src/views/app/AppDetailView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 Web 建立关键路径测试基建，并覆盖登录态、订阅流和价格历史查看</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/vitest.config.ts, apps/web/test/setup.ts, apps/web/test/auth-session.test.ts, apps/web/test/profile-subscription.test.ts, apps/web/test/app-history.test.ts, apps/web/src/router.ts, apps/web/src/lib/auth-session.ts, apps/web/src/composables/useAuthSession.ts, apps/web/src/composables/useAuthedApi.ts, apps/web/src/composables/usePriceHistory.ts</files>
  <read_first>apps/web/package.json, apps/web/src/router.ts, apps/web/src/lib/auth-session.ts, apps/web/src/composables/useAuthSession.ts, apps/web/src/composables/useAuthedApi.ts, apps/web/src/composables/usePriceHistory.ts, apps/web/src/views/auth/AuthView.vue, apps/web/src/views/profile/ProfileView.vue, apps/web/src/views/app/AppDetailView.vue</read_first>
  <action>Install and lock the Web testing dependencies with an explicit pnpm workspace command such as `pnpm add -D --filter @appstore-price-radar/web vitest @vue/test-utils jsdom`, ensuring both `apps/web/package.json` and root `pnpm-lock.yaml` are updated. Add an exact `test` script `vitest run` to `apps/web/package.json`. Create `apps/web/vitest.config.ts` and `apps/web/test/setup.ts` to provide a browser-like environment, router-aware mounting helpers, and fetch/localStorage mocks. Write `apps/web/test/auth-session.test.ts` to cover stored-session restore and unauthenticated redirect behavior around `useAuthSession()` / `router.beforeEach(...)`. Write `apps/web/test/profile-subscription.test.ts` to cover at least one successful subscription-list or create flow in `ProfileView.vue`, including auth-required request behavior and success/error feedback. Write `apps/web/test/app-history.test.ts` to cover `usePriceHistory()` / `AppDetailView.vue` initial load, cached reload, and load-more path (including an error or abort branch). Keep production code edits limited to `apps/web/src/router.ts`, `apps/web/src/lib/auth-session.ts`, `apps/web/src/composables/useAuthSession.ts`, `apps/web/src/composables/useAuthedApi.ts`, and `apps/web/src/composables/usePriceHistory.ts`; do not modify the view SFCs unless mounting them is impossible without a minimal test seam.</action>
  <verify>pnpm --filter @appstore-price-radar/web test</verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `"test": "vitest run"`
    - `apps/web/package.json` contains `"@vue/test-utils"`
    - `apps/web/package.json` contains `"jsdom"`
    - `pnpm-lock.yaml` contains `@vue/test-utils`
    - `pnpm-lock.yaml` contains `jsdom`
    - `apps/web/vitest.config.ts` contains `defineConfig`
    - `apps/web/test/auth-session.test.ts` contains `redirect`
    - `apps/web/test/profile-subscription.test.ts` contains `ProfileView`
    - `apps/web/test/app-history.test.ts` contains `loadMore`
  </acceptance_criteria>
  <done>前端关键路径终于有了自动化保护，后续迭代不再完全依赖手工点页面回归。</done>
</task>

<task type="auto">
  <name>Task 2: 在根脚本定义统一验证入口，并把 Web/Worker/Smoke path 串成官方发布前命令</name>
  <files>package.json, README.md, apps/web/package.json</files>
  <read_first>package.json, README.md, apps/web/package.json, apps/worker/package.json, .planning/phases/05-feed-performance-regression/05-VALIDATION.md</read_first>
  <action>Update the root `package.json` to add an exact `test` script that runs both workspace test suites, plus exact `verify` and `verify:full` scripts that chain `pnpm typecheck`, `pnpm lint`, the root `pnpm test`, and `pnpm --filter @appstore-price-radar/worker test:smoke` in a deterministic release order. Keep `apps/web/package.json` and `apps/worker/package.json` script names compatible with that root flow. Update `README.md` to document the new official release-validation command and explicitly state that deploys should use `pnpm verify` (or `pnpm verify:full` if you choose both names) before shipping. Do not invent a CI-only command name that is undocumented in the repo root.</action>
  <verify>pnpm verify</verify>
  <acceptance_criteria>
    - `package.json` contains `"test": "pnpm --filter @appstore-price-radar/worker test && pnpm --filter @appstore-price-radar/web test"`
    - `package.json` contains `"verify":`
    - `package.json` contains `"verify:full":`
    - `package.json` contains `test:smoke`
    - `README.md` contains `pnpm verify`
    - `README.md` contains `pnpm --filter @appstore-price-radar/worker test:smoke`
  </acceptance_criteria>
  <done>团队已经有了单一官方验证入口，部署前不再依赖“记住一串命令”的隐式流程。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/web test`
- [ ] `pnpm verify`
- [ ] `rg -n "\"test\": \"vitest run\"|@vue/test-utils|jsdom|pnpm verify|verify:full|test:smoke" package.json README.md apps/web/package.json apps/web/test apps/web/vitest.config.ts`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/05-feed-performance-regression/05-03-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- Web 关键路径测试基建已经接入
- 登录态恢复、订阅流和价格历史查看都拥有自动化保护
- 根脚本提供单一官方验证入口
- 部署前验证路径包含 typecheck、lint、tests 和 smoke path

</success_criteria>

<output>
After completion, create `.planning/phases/05-feed-performance-regression/05-03-SUMMARY.md`
</output>
