---
phase: 05-feed-performance-regression
plan: '02'
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/worker/src/index.ts
  - apps/worker/src/routes/auth.ts
  - apps/worker/src/routes/subscriptions.ts
  - apps/worker/src/services/auth.ts
  - apps/worker/src/services/subscriptions.ts
  - apps/worker/src/services/jobs.ts
  - apps/worker/test/auth.routes.test.ts
  - apps/worker/test/subscriptions.routes.test.ts
  - apps/worker/test/jobs.check-route.test.ts
  - apps/worker/test/auth.service.test.ts
  - apps/worker/test/subscriptions.service.test.ts
autonomous: true
requirements:
  - QA-01
user_setup: []
must_haves:
  truths:
    - Worker 的关键风险不只在纯 service 逻辑，还在 HTTP 层的鉴权、query/schema 解析、状态码与错误文案；测试必须覆盖 route 与 service 两层。
    - 手动巡检入口必须继续保持 deny-by-default 行为：未开启、缺少 `CRON_SECRET`、secret 不匹配、任务被跳过都要有稳定响应。
  artifacts:
    - apps/worker/test/auth.routes.test.ts
    - apps/worker/test/subscriptions.routes.test.ts
    - apps/worker/test/jobs.check-route.test.ts
    - apps/worker/test/auth.service.test.ts
    - apps/worker/test/subscriptions.service.test.ts
  key_links:
    - `apps/worker/src/index.ts` 是 `/api/jobs/check`、`app.notFound()` 和 `app.onError()` 的真实入口，路由级回归必须直接覆盖这里的行为。
    - `apps/worker/src/routes/auth.ts` 与 `apps/worker/src/routes/subscriptions.ts` 已经是前端真实调用面，因此回归不能只停留在 helper 函数。
---

<objective>
补齐 Worker 关键路由和服务的回归测试，让认证、订阅、公开 feed 和手动巡检鉴权这些高风险入口在继续迭代时都有稳定的自动化保护。

Purpose: `QA-01` 的重点是“关键路由与服务都有回归”，因此本计划会把 route-level 行为和 service-level 状态逻辑分成两个任务，避免测试只覆盖一半。
Output: auth/subscriptions/jobs 的 route 回归，auth/subscriptions 的 service 回归，以及必要的 test seam 导出。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-feed-performance-regression/05-RESEARCH.md
@.planning/phases/05-feed-performance-regression/05-VALIDATION.md
@apps/worker/src/index.ts
@apps/worker/src/routes/auth.ts
@apps/worker/src/routes/subscriptions.ts
@apps/worker/src/services/auth.ts
@apps/worker/src/services/subscriptions.ts
@apps/worker/src/services/jobs.ts
@apps/worker/test/auth.test.ts
@apps/worker/test/checker.price-change.test.ts
@apps/worker/test/scheduler.rate-limit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 auth / subscriptions / jobs 入口建立 request-level 回归测试</name>
  <files>apps/worker/src/index.ts, apps/worker/src/routes/auth.ts, apps/worker/src/routes/subscriptions.ts, apps/worker/test/auth.routes.test.ts, apps/worker/test/subscriptions.routes.test.ts, apps/worker/test/jobs.check-route.test.ts</files>
  <read_first>apps/worker/src/index.ts, apps/worker/src/routes/auth.ts, apps/worker/src/routes/subscriptions.ts, apps/worker/src/middleware/auth.ts, apps/worker/src/services/jobs.ts, apps/worker/test/public.route.test.ts, apps/worker/test/scheduler.rate-limit.test.ts</read_first>
  <action>Create `apps/worker/test/auth.routes.test.ts` and cover at least: unauthorized `/api/auth/me`, invalid payload rejection for `/api/auth/login` or `/api/auth/register`, successful `/api/auth/me` DTO shape when auth context is present, and `/api/auth/logout` response shape. Create `apps/worker/test/subscriptions.routes.test.ts` and cover at least: auth-required protection on `/api/subscriptions`, successful list/create/delete JSON shapes, and not-found delete behavior. Create `apps/worker/test/jobs.check-route.test.ts` and cover all four manual job gate branches from `apps/worker/src/index.ts`: `404 Not Found` when `MANUAL_PRICE_CHECKS_ENABLED=false`, `503` when enabled but `CRON_SECRET` missing, `401 Unauthorized` when secret mismatch, and `202` when `runProtectedPriceCheck(...)` returns `{ kind: 'skipped' }`. If test setup needs it, keep `export { app }` in `apps/worker/src/index.ts` and add only minimal seams required for `app.request(...)` without changing production behavior.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/auth.routes.test.ts test/subscriptions.routes.test.ts test/jobs.check-route.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/auth.routes.test.ts` contains `/api/auth/me`
    - `apps/worker/test/auth.routes.test.ts` contains `/api/auth/logout`
    - `apps/worker/test/subscriptions.routes.test.ts` contains `/api/subscriptions`
    - `apps/worker/test/jobs.check-route.test.ts` contains `CRON_SECRET is required when manual price checks are enabled`
    - `apps/worker/test/jobs.check-route.test.ts` contains `Unauthorized`
    - `apps/worker/test/jobs.check-route.test.ts` contains `price-check-already-running`
  </acceptance_criteria>
  <done>Worker 的关键 HTTP surface 已经拥有请求/响应级回归，一旦路由鉴权、状态码或错误文案漂移就会立即暴露。</done>
</task>

<task type="auto">
  <name>Task 2: 为 auth / subscriptions 核心状态逻辑补 service-level 回归，锁住高副作用分支</name>
  <files>apps/worker/src/services/auth.ts, apps/worker/src/services/subscriptions.ts, apps/worker/test/auth.service.test.ts, apps/worker/test/subscriptions.service.test.ts</files>
  <read_first>apps/worker/src/services/auth.ts, apps/worker/src/services/subscriptions.ts, apps/worker/src/services/auth.types.ts, apps/worker/src/services/subscriptions.types.ts, apps/worker/test/auth.test.ts, apps/worker/test/checker.price-change.test.ts</read_first>
  <action>Create `apps/worker/test/auth.service.test.ts` to cover concrete high-risk branches in `apps/worker/src/services/auth.ts`: password login invalid credential path, login code or reset token rotation/invalid reuse path, and `revokeSession(...)` removing the current session. Create `apps/worker/test/subscriptions.service.test.ts` to cover `createUserSubscription(...)`, `listUserSubscriptions(...)`, and `deleteUserSubscription(...)` with deterministic mocked DB state: subscription create/upsert response DTO shape, list ordering/field mapping through `toSubscriptionItemDto`, delete success path, and delete 404 path for an already-removed or missing subscription. Reuse existing mock style from current worker tests (`vi.hoisted`, in-memory db state, mocked `getDb`) instead of introducing another testing framework.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/auth.service.test.ts test/subscriptions.service.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/auth.service.test.ts` contains `revokeSession`
    - `apps/worker/test/auth.service.test.ts` contains `invalid credentials`
    - `apps/worker/test/subscriptions.service.test.ts` contains `createUserSubscription`
    - `apps/worker/test/subscriptions.service.test.ts` contains `listUserSubscriptions`
    - `apps/worker/test/subscriptions.service.test.ts` contains `deleteUserSubscription`
    - `apps/worker/test/subscriptions.service.test.ts` contains `toSubscriptionItemDto`
  </acceptance_criteria>
  <done>认证和订阅的高副作用状态逻辑也被锁住了，Phase 5 的 Worker 回归保护不再只停留在 HTTP 表层。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/auth.routes.test.ts test/subscriptions.routes.test.ts test/jobs.check-route.test.ts`
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/auth.service.test.ts test/subscriptions.service.test.ts`
- [ ] `rg -n "/api/auth/me|/api/subscriptions|CRON_SECRET is required when manual price checks are enabled|revokeSession|createUserSubscription|deleteUserSubscription" apps/worker/test`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/05-feed-performance-regression/05-02-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- Auth / subscriptions / jobs 的关键入口具备请求级回归
- Auth / subscriptions 的核心状态逻辑具备 service-level 回归
- `/api/jobs/check` 的 deny-by-default 门禁不再依赖人工验证
- Worker 高风险 HTTP surface 的错误文案与状态码被锁住

</success_criteria>

<output>
After completion, create `.planning/phases/05-feed-performance-regression/05-02-SUMMARY.md`
</output>
