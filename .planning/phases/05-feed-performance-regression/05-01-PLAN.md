---
phase: 05-feed-performance-regression
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/services/public.ts
  - apps/worker/src/services/public.types.ts
  - apps/worker/src/routes/public.ts
  - apps/worker/src/index.ts
  - apps/worker/test/public.drops.test.ts
  - apps/worker/test/public.route.test.ts
autonomous: true
requirements:
  - API-03
  - QA-01
user_setup: []
must_haves:
  truths:
    - 公开降价流必须在数据增长后仍能稳定返回按 `(appId, country)` 去重后的最近降价结果，不能继续依赖超量抓取后在内存里裁剪。
    - 每条公开 feed 项的 `submissionCount` 必须与当前活跃订阅数保持一致，且响应字段名不能破坏现有首页消费方式。
  artifacts:
    - apps/worker/src/services/public.ts
    - apps/worker/src/routes/public.ts
    - apps/worker/test/public.drops.test.ts
    - apps/worker/test/public.route.test.ts
  key_links:
    - `apps/web/src/views/home/HomeView.vue` 当前固定消费 `/api/public/drops?limit=120&dedupe=1`，因此 `items[].submissionCount`、`detectedAt` 和去重语义必须保持稳定。
    - `apps/worker/src/db/schema.ts` 已提供 `app_drop_events_app_country_detected_idx` 与 `subscriptions` 表定义，公开 feed 优化应优先复用现有 schema，而不是引入额外持久化层。
---

<objective>
收敛公开降价流的查询与聚合策略，让 `/api/public/drops` 在数据增长后仍能以稳定成本返回正确的最近降价列表与关注人数，同时为后续 Phase 5 测试计划提供可验证的行为基线。

Purpose: `API-03` 的核心不是单纯“更快一点”，而是把 dedupe、排序、limit 与 submissionCount 的语义固定到数据库查询边界上，再用测试锁住。
Output: 更稳定的 `getPublicDrops(...)` 实现、公开 feed 路由回归测试、公开 feed service 回归测试。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-feed-performance-regression/05-RESEARCH.md
@.planning/phases/05-feed-performance-regression/05-VALIDATION.md
@apps/worker/src/db/schema.ts
@apps/worker/src/constants/routes.ts
@apps/worker/src/services/public.ts
@apps/worker/src/services/public.types.ts
@apps/worker/src/routes/public.ts
@apps/worker/src/index.ts
@apps/web/src/views/home/HomeView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把公开 feed 的 dedupe 和关注人数聚合收敛到数据库查询边界</name>
  <files>apps/worker/src/services/public.ts, apps/worker/src/services/public.types.ts</files>
  <read_first>apps/worker/src/services/public.ts, apps/worker/src/services/public.types.ts, apps/worker/src/db/schema.ts, apps/worker/src/constants/routes.ts, apps/web/src/views/home/HomeView.vue</read_first>
  <action>Rewrite `getPublicDrops(...)` in `apps/worker/src/services/public.ts` so it no longer relies on `PUBLIC_DROPS_FETCH_MULTIPLIER` over-fetch + in-memory `Set` dedupe as the primary correctness mechanism. Implement a DB-side latest-drop selection that returns exactly one newest drop row per `(appId, country)` pair, ordered by `detectedAt DESC` and a deterministic tie-breaker (`id DESC` if needed), then apply `limit` after dedupe. Keep the existing response shape `items[].id/appId/country/appName/storeUrl/iconUrl/currency/oldPrice/newPrice/dropPercent/detectedAt/submissionCount` unchanged for `HomeView.vue`. Update the subscription count aggregation so it counts only `subscriptions.isActive = true` rows for the final deduped `(appId, country)` set and maps them back to `submissionCount` without a growing `or(...)` chain over the raw over-fetched event list. If type aliases in `apps/worker/src/services/public.types.ts` need tightening, make `PublicDropItem` an explicit DTO-like object shape rather than a loose extension of the full Drizzle row.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/public.drops.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/src/services/public.ts` no longer contains `PUBLIC_DROPS_FETCH_MULTIPLIER`
    - `apps/worker/src/services/public.ts` contains `submissionCount`
    - `apps/worker/src/services/public.ts` contains `isActive`
    - `apps/worker/src/services/public.ts` contains `limit(finalLimit)`
    - `apps/worker/src/services/public.types.ts` contains `submissionCount: number`
  </acceptance_criteria>
  <done>公开 feed 的排序、去重和关注人数聚合都在查询边界上稳定下来，首页不再依赖“超量抓取后裁剪”的隐式假设。</done>
</task>

<task type="auto">
  <name>Task 2: 为公开 feed 建立 service + route 双层回归，锁住 dedupe、query 默认值和响应结构</name>
  <files>apps/worker/src/routes/public.ts, apps/worker/src/index.ts, apps/worker/test/public.drops.test.ts, apps/worker/test/public.route.test.ts</files>
  <read_first>apps/worker/src/routes/public.ts, apps/worker/src/index.ts, apps/worker/src/services/public.ts, apps/worker/src/services/public.types.ts, apps/worker/test/checker.price-change.test.ts, apps/worker/test/scheduler.rate-limit.test.ts</read_first>
  <action>Create `apps/worker/test/public.drops.test.ts` to cover at least these cases with deterministic mocked data: newest-per-pair dedupe, descending order by latest detection time, correct `submissionCount` for active subscriptions only, and `country` filter behavior. Create `apps/worker/test/public.route.test.ts` to call the Hono app through `app.request(...)`; if needed, export `app` from `apps/worker/src/index.ts` specifically for tests. In the route test, assert `/api/public/drops` default query behavior (`dedupe=true`, default limit), `country=US` filtering, `limit` clamping via schema, HTTP 200 response, and presence of exact response keys `items`, `appId`, `country`, `appName`, `newPrice`, `submissionCount`, and `detectedAt`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/public.drops.test.ts test/public.route.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/public.drops.test.ts` contains `submissionCount`
    - `apps/worker/test/public.drops.test.ts` contains `country filter`
    - `apps/worker/test/public.route.test.ts` contains `/api/public/drops`
    - `apps/worker/test/public.route.test.ts` contains `dedupe`
    - `apps/worker/test/public.route.test.ts` contains `submissionCount`
    - `apps/worker/src/index.ts` contains `export { app }`
  </acceptance_criteria>
  <done>公开 feed 的 service 与 route 语义都被自动化测试锁住，后续 Phase 5 再补 QA-01 时可以直接站在这条基线上扩展。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/public.drops.test.ts test/public.route.test.ts`
- [ ] `rg -n "PUBLIC_DROPS_FETCH_MULTIPLIER|submissionCount|export \\{ app \\}|/api/public/drops" apps/worker/src apps/worker/test`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/05-feed-performance-regression/05-01-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- `/api/public/drops` 不再依赖超量抓取后的内存去重维持正确性
- `submissionCount` 与活跃订阅数聚合语义被锁住
- 公开 feed 的 service 与 route 都有回归测试
- 首页消费的响应字段不发生破坏性变化

</success_criteria>

<output>
After completion, create `.planning/phases/05-feed-performance-regression/05-01-SUMMARY.md`
</output>
