---
phase: 01-data-baseline-deployability
plan: '03'
type: execute
wave: 2
depends_on:
  - '01-01'
files_modified:
  - apps/worker/test/schema.bootstrap.test.ts
  - apps/worker/test/fresh-install.smoke.test.ts
  - apps/worker/package.json
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - Maintainers can run a deterministic smoke command that proves worker bootstrap and the subscription-to-history path still work after a fresh setup.
    - Phase 1 regression tests fail fast if baseline SQL drifts away from the runtime schema again.
  artifacts:
    - apps/worker/test/schema.bootstrap.test.ts
    - apps/worker/test/fresh-install.smoke.test.ts
    - apps/worker/package.json
  key_links:
    - apps/worker/package.json exposes a test:smoke command that runs both new test files.
    - The new smoke tests reuse existing worker mocks instead of calling real Apple or Resend services.
---

<objective>
建立可重复执行的 smoke verification 基线，确保 fresh install 后最小 API 闭环可以被 deterministic 地证明。

Purpose: DATA-02 需要的不只是“有测试”，而是“维护者能在 Phase 1 之后反复验证 fresh install 的关键路径”。这一步也会把 schema 漂移重新引入时的反馈时间降到分钟级。
Output: 两个新的 worker smoke/regression tests，以及 `apps/worker/package.json` 中的 `test:smoke` 命令。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-baseline-deployability/01-CONTEXT.md
@.planning/phases/01-data-baseline-deployability/01-RESEARCH.md
@apps/worker/src/index.ts
@apps/worker/src/routes/subscriptions.ts
@apps/worker/src/routes/prices.ts
@apps/worker/src/services/subscriptions.ts
@apps/worker/src/services/prices.ts
@apps/worker/src/lib/checker.ts
@apps/worker/src/lib/auth.ts
@apps/worker/test/auth.test.ts
@apps/worker/test/checker.price-change.test.ts
@apps/worker/test/scheduler.rate-limit.test.ts
@apps/worker/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a schema bootstrap regression test for the canonical SQL assets</name>
  <files>apps/worker/test/schema.bootstrap.test.ts</files>
  <read_first>apps/worker/drizzle/0000_init.sql, apps/worker/drizzle/0001_price_change_events.sql, apps/worker/src/db/schema.ts, apps/worker/test/scheduler.rate-limit.test.ts</read_first>
  <action>Create `apps/worker/test/schema.bootstrap.test.ts` as a Vitest file that reads `apps/worker/drizzle/0000_init.sql` and `apps/worker/drizzle/0001_price_change_events.sql` from disk and asserts: `0000_init.sql` contains `CREATE TABLE IF NOT EXISTS app_price_change_events`, `request_id varchar(96) NOT NULL`, and `CREATE UNIQUE INDEX IF NOT EXISTS app_price_change_events_app_request_uniq`; `0001_price_change_events.sql` contains the guarded legacy backfill (`to_regclass('app_price_history') IS NOT NULL`) and still keeps `ON CONFLICT (app_id, country, request_id) DO NOTHING`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/schema.bootstrap.test.ts</verify>
  <acceptance_criteria>
    - `apps/worker/test/schema.bootstrap.test.ts` exists
    - `apps/worker/test/schema.bootstrap.test.ts` contains `describe('schema bootstrap contract'`
    - `apps/worker/test/schema.bootstrap.test.ts` contains `CREATE TABLE IF NOT EXISTS app_price_change_events`
  </acceptance_criteria>
  <done>The repository has a deterministic regression test that fails immediately if Phase 1 baseline SQL drifts again.</done>
</task>

<task type="auto">
  <name>Task 2: Add a request-level fresh install smoke test and expose it as test:smoke</name>
  <files>apps/worker/test/fresh-install.smoke.test.ts, apps/worker/package.json</files>
  <read_first>apps/worker/src/index.ts, apps/worker/src/routes/subscriptions.ts, apps/worker/src/routes/prices.ts, apps/worker/src/services/subscriptions.ts, apps/worker/src/lib/checker.ts, apps/worker/src/lib/auth.ts, apps/worker/test/checker.price-change.test.ts, apps/worker/package.json</read_first>
  <action>Create `apps/worker/test/fresh-install.smoke.test.ts` as a Vitest file that uses `worker.fetch` from `apps/worker/src/index.ts`, mocks DB / App Store / email boundaries using the same style as `apps/worker/test/checker.price-change.test.ts`, and verifies four deterministic requests: `GET /api/health` returns 200 with `{ ok: true }`; authenticated `POST /api/subscriptions` returns 200; `POST /api/jobs/check` with `x-cron-secret` returns 200; `GET /api/prices/:appId?country=US` returns `snapshot` and `history`. Then update `apps/worker/package.json` to add `\"test:smoke\": \"vitest run test/schema.bootstrap.test.ts test/fresh-install.smoke.test.ts\"`.</action>
  <verify>pnpm --filter @appstore-price-radar/worker test -- test/fresh-install.smoke.test.ts && pnpm --filter @appstore-price-radar/worker test:smoke</verify>
  <acceptance_criteria>
    - `apps/worker/test/fresh-install.smoke.test.ts` exists
    - `apps/worker/test/fresh-install.smoke.test.ts` contains `describe('fresh install smoke'`
    - `apps/worker/package.json` contains `"test:smoke": "vitest run test/schema.bootstrap.test.ts test/fresh-install.smoke.test.ts"`
  </acceptance_criteria>
  <done>Maintainers can run one deterministic command that proves the worker bootstrap and minimal subscription/history path still work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/schema.bootstrap.test.ts`
- [ ] `pnpm --filter @appstore-price-radar/worker test -- test/fresh-install.smoke.test.ts`
- [ ] `pnpm --filter @appstore-price-radar/worker test:smoke`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-data-baseline-deployability/01-03-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- The repository exposes a single deterministic smoke command for Phase 1
- New regressions fail on schema drift or broken bootstrap wiring
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-data-baseline-deployability/01-03-SUMMARY.md`
</output>
