---
phase: 01-data-baseline-deployability
plan: '02'
type: execute
wave: 3
depends_on:
  - '01-01'
  - '01-03'
files_modified:
  - README.md
  - .env.example
  - apps/worker/.dev.vars.example
autonomous: true
requirements:
  - DATA-03
user_setup: []
must_haves:
  truths:
    - Maintainers see one official database bootstrap path and one smoke verification command in project docs.
    - Production docs no longer describe CRON_SECRET as optional when /api/jobs/check is exposed.
  artifacts:
    - README.md
    - .env.example
    - apps/worker/.dev.vars.example
  key_links:
    - README.md references the same worker env contract described in apps/worker/.dev.vars.example.
    - README.md references the same smoke verification command exposed by apps/worker/package.json.
---

<objective>
把 Phase 1 的建库路径、环境变量边界和 smoke verification 方式固化到 README 与 env examples，形成唯一对外说明。

Purpose: DATA-03 的核心不是“文档更多”，而是“文档不再互相矛盾”。等 schema baseline 和 smoke command 落地后，再把这套真相写回操作手册。
Output: 对齐后的 `README.md`、`.env.example`、`apps/worker/.dev.vars.example`。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-baseline-deployability/01-CONTEXT.md
@.planning/phases/01-data-baseline-deployability/01-RESEARCH.md
@README.md
@.env.example
@apps/worker/.dev.vars.example
@apps/worker/package.json
@apps/worker/wrangler.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the bootstrap and deployment docs around the canonical worker path</name>
  <files>README.md</files>
  <read_first>README.md, apps/worker/package.json, apps/worker/drizzle.config.ts, apps/worker/wrangler.toml</read_first>
  <action>Update `README.md` so the official database bootstrap path is `pnpm --filter @appstore-price-radar/worker db:push`, the official smoke command is `pnpm --filter @appstore-price-radar/worker test:smoke`, and the production deployment section explicitly states `CRON_SECRET` is required whenever `POST /api/jobs/check` remains enabled. Remove or demote the wording that tells fresh installs to run only `apps/worker/drizzle/0000_init.sql` directly as the primary setup path.</action>
  <verify>rg -n "db:push|test:smoke|CRON_SECRET" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `pnpm --filter @appstore-price-radar/worker db:push`
    - `README.md` contains `pnpm --filter @appstore-price-radar/worker test:smoke`
    - `README.md` contains `CRON_SECRET` and `required` in the production deployment guidance
  </acceptance_criteria>
  <done>README exposes one canonical bootstrap flow and one smoke verification command, with production security prerequisites called out explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Align env examples with the same runtime contract</name>
  <files>.env.example, apps/worker/.dev.vars.example</files>
  <read_first>.env.example, apps/worker/.dev.vars.example, README.md, apps/worker/src/env.ts, apps/worker/src/index.ts</read_first>
  <action>Update `.env.example` and `apps/worker/.dev.vars.example` so the root file continues to document only `VITE_API_BASE`, the worker example clearly documents `DATABASE_URL`, `APP_BASE_URL`, `CORS_ORIGIN`, and changes the `CRON_SECRET` comment from `recommended` to `required in production for POST /api/jobs/check`. Keep the wording consistent with `README.md` and `apps/worker/src/env.ts` validation rules.</action>
  <verify>rg -n "VITE_API_BASE|DATABASE_URL|APP_BASE_URL|CORS_ORIGIN|CRON_SECRET" .env.example apps/worker/.dev.vars.example</verify>
  <acceptance_criteria>
    - `.env.example` contains `# Web only`
    - `apps/worker/.dev.vars.example` contains `CRON_SECRET=` and `required in production`
    - `apps/worker/.dev.vars.example` contains `APP_BASE_URL=` and `CORS_ORIGIN=`
  </acceptance_criteria>
  <done>Env examples describe the same split and production constraints as the updated README.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "db:push|test:smoke|CRON_SECRET" README.md apps/worker/.dev.vars.example`
- [ ] `rg -n "VITE_API_BASE" .env.example`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-data-baseline-deployability/01-02-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- README, root env example, and worker env example describe the same bootstrap and deployment contract
- CRON_SECRET is documented as production-required, not optional
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-data-baseline-deployability/01-02-SUMMARY.md`
</output>
