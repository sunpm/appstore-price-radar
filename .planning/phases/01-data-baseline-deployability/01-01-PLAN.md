---
phase: 01-data-baseline-deployability
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/drizzle/0000_init.sql
  - apps/worker/drizzle/0001_price_change_events.sql
  - apps/worker/src/db/schema.ts
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - Fresh-install baseline SQL creates every runtime table that current worker code reads and writes, including app_price_change_events.
    - Legacy upgrade SQL no longer acts as the only way to create app_price_change_events.
  artifacts:
    - apps/worker/drizzle/0000_init.sql
    - apps/worker/drizzle/0001_price_change_events.sql
  key_links:
    - apps/worker/src/db/schema.ts and apps/worker/drizzle/0000_init.sql describe the same change-event table shape.
    - apps/worker/drizzle/0001_price_change_events.sql only backfills legacy app_price_history data into app_price_change_events.
---

<objective>
收敛数据库初始化基线，让 fresh install 不再依赖隐含的第二步迁移才能拥有当前运行时代码需要的表结构。

Purpose: 先修掉 `schema.ts`、`0000_init.sql` 和 `0001_price_change_events.sql` 之间的漂移根因，为后续 smoke verification 和 README 修订提供可信基线。
Output: 对齐后的 `0000_init.sql`、legacy-safe 的 `0001_price_change_events.sql`，以及与运行时代码保持一致的 schema contract。
</objective>

<execution_context>
@/Users/sunpm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sunpm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-baseline-deployability/01-CONTEXT.md
@.planning/phases/01-data-baseline-deployability/01-RESEARCH.md
@apps/worker/src/db/schema.ts
@apps/worker/drizzle/0000_init.sql
@apps/worker/drizzle/0001_price_change_events.sql
@apps/worker/drizzle.config.ts
@apps/worker/src/services/prices.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the baseline SQL to match the current runtime schema</name>
  <files>apps/worker/drizzle/0000_init.sql, apps/worker/src/db/schema.ts</files>
  <read_first>apps/worker/drizzle/0000_init.sql, apps/worker/src/db/schema.ts, apps/worker/src/services/prices.ts</read_first>
  <action>Update `apps/worker/drizzle/0000_init.sql` so the fresh-install baseline creates the current runtime tables and indexes from `apps/worker/src/db/schema.ts`, including `app_price_change_events` with columns `old_amount`, `new_amount`, `changed_at`, `source`, and `request_id`, plus indexes `app_price_change_events_app_changed_idx` and `app_price_change_events_app_request_uniq`. Keep `app_price_history` only if the file still needs to preserve legacy backfill compatibility, but do not leave `app_price_change_events` absent from the baseline.</action>
  <verify>rg -n "CREATE TABLE IF NOT EXISTS app_price_change_events|request_id varchar\\(96\\) NOT NULL|app_price_change_events_app_request_uniq" apps/worker/drizzle/0000_init.sql</verify>
  <acceptance_criteria>
    - `apps/worker/drizzle/0000_init.sql` contains `CREATE TABLE IF NOT EXISTS app_price_change_events`
    - `apps/worker/drizzle/0000_init.sql` contains `request_id varchar(96) NOT NULL`
    - `apps/worker/drizzle/0000_init.sql` contains `CREATE UNIQUE INDEX IF NOT EXISTS app_price_change_events_app_request_uniq`
  </acceptance_criteria>
  <done>Fresh-install baseline SQL defines the same change-event table shape that runtime code reads and writes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the follow-up migration into a legacy-safe backfill step</name>
  <files>apps/worker/drizzle/0001_price_change_events.sql</files>
  <read_first>apps/worker/drizzle/0001_price_change_events.sql, apps/worker/drizzle/0000_init.sql, apps/worker/src/services/prices.ts</read_first>
  <action>Refactor `apps/worker/drizzle/0001_price_change_events.sql` so it no longer behaves like the only place that creates `app_price_change_events`. Keep the legacy backfill from `app_price_history`, but wrap that backfill in a `DO $$ ... IF to_regclass('app_price_history') IS NOT NULL THEN ... END IF; END $$;` guard and preserve `ON CONFLICT (app_id, country, request_id) DO NOTHING` so upgraded installs remain idempotent after the new `0000_init.sql` baseline already defines the table.</action>
  <verify>rg -n "to_regclass\\('app_price_history'\\)|ON CONFLICT \\(app_id, country, request_id\\) DO NOTHING" apps/worker/drizzle/0001_price_change_events.sql</verify>
  <acceptance_criteria>
    - `apps/worker/drizzle/0001_price_change_events.sql` contains `to_regclass('app_price_history') IS NOT NULL`
    - `apps/worker/drizzle/0001_price_change_events.sql` contains `ON CONFLICT (app_id, country, request_id) DO NOTHING`
    - `apps/worker/drizzle/0001_price_change_events.sql` no longer needs to be the first place that creates `app_price_change_events`
  </acceptance_criteria>
  <done>Legacy upgrade SQL backfills old history safely without reintroducing fresh-install schema drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @appstore-price-radar/worker typecheck`
- [ ] `rg -n "CREATE TABLE IF NOT EXISTS app_price_change_events|request_id" apps/worker/drizzle/0000_init.sql apps/worker/drizzle/0001_price_change_events.sql`
- [ ] `node /Users/sunpm/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-data-baseline-deployability/01-01-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- Fresh-install baseline SQL contains the change-event table and indexes used by runtime code
- Follow-up migration is safe to run against both legacy and rebuilt environments
- No errors or warnings introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-data-baseline-deployability/01-01-SUMMARY.md`
</output>
