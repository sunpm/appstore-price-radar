[build]
  base = "."
  command = "pnpm --filter @appstore-price-radar/web build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.9.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
