{
  "name": "@appstore-price-radar/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest run",
    "test:smoke": "vitest run test/schema.bootstrap.test.ts test/fresh-install.smoke.test.ts",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@appstore-price-radar/contracts": "workspace:*",
    "@hono/zod-validator": "^0.4.3",
    "@neondatabase/serverless": "^0.10.4",
    "@noble/hashes": "^2.0.1",
    "drizzle-orm": "^0.44.5",
    "hono": "^4.9.10",
    "resend": "^6.2.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260215.0",
    "dotenv": "^16.6.1",
    "drizzle-kit": "^0.31.7",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.9.1"
  }
}
