# Web only
# This root `.env` is only for frontend vars.
# Do not put worker secrets or runtime vars here.
# Worker vars are managed in `apps/worker/.dev.vars`.
# Worker base URL for frontend requests (required)
VITE_API_BASE=http://127.0.0.1:8787
